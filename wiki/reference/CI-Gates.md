# CI gates reference

This page lists every gate in the deterministic battery. It explains the invariant each gate proves and identifies the script behind it. Three per-OS workflows run in parallel. Run the deterministic subset locally in one command before you commit:

```bash
bash scripts/check-all.sh
```

**Worktree-native flow (ratified 2026-07-06): the full battery gates on `pull_request`, not on a plain `push` to `main`.** The `main` branch only merges a PR after its required checks pass. A plain push to `main` runs almost nothing. In `tests-linux.yml`, the single `syntax` job (`bash -n` and AST-parsing every `.ps1`) runs unconditionally. Every other job in all three per-OS workflows gates on `github.event_name == 'pull_request'`, `workflow_dispatch`, or the nightly `workflow_call`. This includes the `install-smoke` job in both `tests-mac.yml` and `tests-windows.yml`. The `[T] CI All` aggregate badge workflow carries no `push` trigger at all. It runs only on `pull_request`. The `pull_request` trigger itself carries no `paths-ignore` filter. A trigger-level filter that skips a required check would block the merge of a docs-only PR forever. The docs/wiki diet returns as a job-level no-op instead.

## ⚡ Quick Reference

| Workflow | Runs on | Jobs |
|---|---|---|
| [`[T] Linux Tests`](https://github.com/alexherrero/agentm/blob/main/.github/workflows/tests-linux.yml) | `ubuntu-latest` | install-smoke + adapter-parity + validate + check-references + check-wiki + unit tests + **verify-v4** + **check-vendored-parity** (vault-lock + hook-config modes) + **check-storage-rules** + **check-vocabulary-membership** (self-test) + **check-vault-frontmatter** (self-test mode) + **verify-orchestration-briefing** + **verify-idle-chain** + **verify-hook-resolution** + **verify-state-routing** + **verify-reflection** + **verify-dreaming** + **verify-auto-org-meters** + **verify-mcp-surface** + **health-score-determinism** + **validate-audit-coverage** + **run-ablation-baseline** + **verify-phases** + **verify-memory-roundtrip** + syntax + **lib-parity** (check-vendored-parity's lib mode) + **pii-guardrails** (check-no-pii + gitleaks) + dogfood-workflows |
| [`[T] Mac Tests`](https://github.com/alexherrero/agentm/blob/main/.github/workflows/tests-mac.yml) | `macos-latest` | install-smoke + validate + check-references + unit tests + **verify-v4** + **check-vendored-parity** (vault-lock + hook-config modes) + **verify-orchestration-briefing** + **verify-hook-resolution** + **verify-state-routing** + **verify-reflection** + **verify-dreaming** + **verify-auto-org-meters** + **verify-mcp-surface** + **health-score-determinism** + **validate-audit-coverage** + **run-ablation-baseline** + **verify-phases** + **verify-memory-roundtrip** + syntax (both shells) + **lib-parity** (check-vendored-parity's lib mode) + **check-no-pii** |
| [`[T] Windows Tests`](https://github.com/alexherrero/agentm/blob/main/.github/workflows/tests-windows.yml) | `windows-latest` | install-smoke (pwsh) + installer-boundary + validate + check-references + pwsh syntax + **lib-parity** (check-vendored-parity's lib mode) + **check-vendored-parity** (hook-config mode) + **check-no-pii** + unit tests |

## What each gate proves

| Gate | Invariant | Script |
|---|---|---|
| install-smoke | Fresh install succeeds; re-run is idempotent; `--update` refreshes managed files but preserves user edits to `wiki/` and `AGENTS.md`; test infra never propagates to scratch. | [`scripts/smoke-install-bash.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/smoke-install-bash.sh), [`scripts/smoke-install-pwsh.ps1`](https://github.com/alexherrero/agentm/blob/main/scripts/smoke-install-pwsh.ps1) |
| post-install integrity | Hook-command paths resolve; every `.sh`/`.ps1` parses; bash installer produces bash commands, pwsh installer produces pwsh commands; `settings.json` has the expected schema; `.harness` state files are valid. | [`scripts/check-integrity-bash.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-integrity-bash.sh), [`scripts/check-integrity-pwsh.ps1`](https://github.com/alexherrero/agentm/blob/main/scripts/check-integrity-pwsh.ps1) |
| adapter-parity | Every adapter ships the canonical set of phase-commands, sub-agents, and skills. | [`scripts/check-parity.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-parity.sh) |
| validate | Every TOML, YAML frontmatter, and JSON across `adapters/` and `templates/` parses and has required keys. | [`scripts/validate-adapters.py`](https://github.com/alexherrero/agentm/blob/main/scripts/validate-adapters.py) |
| check-references | Every `harness/<phases\|agents\|skills\|pipelines>/*.md` mentioned in an adapter file exists; phase-spec "dispatch the `<name>` sub-agent / invoke the `<name>` skill" lines point at a canonical spec; `settings-fragment-{bash,pwsh}.json` have matching schemas. | [`scripts/check-references.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-references.py) |
| check-wiki | Diátaxis structural rules (a–k): mode purity, ADR append-only + `Status: accepted\|superseded\|rejected`, orphan-link detection, globally-unique filenames, no banned-headings-per-mode. Runs `--strict` (blocks PRs) when `wiki/.diataxis` is present; warn-only otherwise. Shipped in v0.9.0 as part of the Diátaxis rollout ([Foundations HLD](agentm-foundations-hld)). | [`scripts/check-wiki.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-wiki.py) |
| check-slop | The deterministic anti-slop voice gate over `wiki/`, `--strict` (blocks on error-/warning-tier findings; suggestion-tier never fails). Mechanism-owned by crickets (voice is a crickets-side capability) — this agentm-repo script is a thin delegator that locates the crickets sibling checkout and shells out to its real `check-slop.py` + the one shared rule pack (`src/wiki/skills/diataxis-author/style/voice-rules.json`), so agentm doesn't carry a second copy of the scanning logic. No direct CI step of its own — runs via the `UNIT_WRAPPED` allowlist (`test_check_slop.py` subprocess-invokes it), same pattern as `check-ci-consistency`. | [`scripts/check-slop.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-slop.py) |
| check-kind-taxonomy | Reports every distinct raw `kind:` frontmatter value found in the real vault, its frequency, and kebab-case violations, against `$MEMORY_VAULT_PATH` — graceful-skip (exit 0) when unset or not a directory. **Report-only, never a failing gate** — the real vault's 47-value mess can't be a hard PASS/FAIL yet without an operator normalization call (parked as [agentm issue #273](https://github.com/alexherrero/agentm/issues/273)), so the step's own exit code is always 0 regardless of findings (the same report-only shape `check-slop.py` had before it flipped to `--strict` in `check-all.sh` — Consolidation arc CONS-3). See [Kind-taxonomy registry](Kind-Taxonomy-Registry). | [`scripts/check-kind-taxonomy.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-kind-taxonomy.sh) → [`harness/skills/memory/scripts/kind_registry.py`](https://github.com/alexherrero/agentm/blob/main/harness/skills/memory/scripts/kind_registry.py) |
| syntax | `bash -n` on every `.sh`; PowerShell AST parse on every `.ps1` across repo root + `scripts/` + `templates/` + `adapters/`. | [`scripts/check-syntax.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-syntax.sh), [`scripts/check-syntax.ps1`](https://github.com/alexherrero/agentm/blob/main/scripts/check-syntax.ps1) |
| unit tests | Every `scripts/test_*.py` (auto-discovered) passes — the memory-script logic in isolation. Since filing-v2 part 2a moved machine state to `~/.local/state/agentm/`, the runner is hermetic on that seam: [`run_unit_suite.py`](https://github.com/alexherrero/agentm/blob/main/scripts/run_unit_suite.py) is unittest discovery plus a fresh scratch `$AGENTM_STATE_DIR` per test, and `scripts/conftest.py` gives pytest invocations the same guard — without it, tests would read and write the machine's real engine state. Every `verify-*` script exports its own scratch `$AGENTM_STATE_DIR` script-wide for the same reason. | `(cd scripts && python3 run_unit_suite.py)` |
| check-vendored-parity | One parametrized gate over six vendored/checksum-manifest parity invariants (CONS-1 merge of what were five separate scripts, plus the L6 wiki-publish-render addition — `lib`, `vault-lock`, `wiki-publish-transform`, `storage-seam`, `hook-config`, `workflow` modes): (1) **lib** — `lib/install/` matches its committed checksums; (2) **vault-lock** — `scripts/vault_lock.py` and its vendored twin `harness/skills/memory/scripts/vault_lock.py` are sha256-identical, so the memory skill and the harness core share one canonical lock implementation (testInfra#0); (3) **wiki-publish-transform** — `scripts/wiki_publish_transform.py` (agentm's own dogfooded copy) and `templates/scripts/wiki_publish_transform.py` (the copy install.sh vendors into every installed project) are sha256-identical; (4) **storage-seam** — the same vendoring pattern for `storage_seam.py` + `storage_device_local.py`; (5) **hook-config** — two functions replicated verbatim across the four memory hooks are byte-identical, so a fix applied to only one hook is caught as drift: `_resolve_vault_path` (env → `.agentm-config.json` plugin key → legacy flat key → none; R0.1 / agentmEngine#0) and `_resolve_agentm_python` (the bootstrap that hands the memory scripts a working interpreter — the *probe* itself lives once, in `harness/hooks/lib/resolve-python.sh`, so only the few lines that find and call it are replicated, and gating them stops a hook drifting back to a bare `python3` the way the whole set had done); (6) **workflow** — every templated `templates/.github/workflows/*.yml` is active at the repo root, byte-identical (the local mirror of the `dogfood-workflows` CI job, below). Runs all six by default; `bash scripts/check-vendored-parity.sh <mode>` runs just one. The `vault-lock` and `hook-config` modes run as direct steps in `tests-linux.yml`/`tests-mac.yml`/`tests-windows.yml`; `lib` runs as its own `lib-parity` job. | [`scripts/check-vendored-parity.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-vendored-parity.sh) |
| check-multi-plan-naming | Locks the named-plan naming contract three ways: (1) `scripts/harness_memory.py` still exposes the named-plan resolver surface — both `resolve_active_plan` (the session→plan binder) and `harness_state_dir` (the state-dir enumerator); (2) no curated `harness/*.md` doc hard-asserts a singleton via the narrow deny-pattern — definite-article `the PLAN.md` + possessive `PLAN.md's` — which still permits every legitimate mention (a named `PLAN-<name>.md`, a `PLAN*.md` glob, a `<slug>.PLAN.md` queued file, the `vault-state-path PLAN.md` CLI example, and `PLAN.archive.*`); (3) both session-start hook twins (`harness-context-session-start.{sh,ps1}`) still glob `PLAN-*.md`, so they cannot drift apart and silently lose named-plan discovery at session boot (assertion 3, added V5-10 part 1 task 5). Scans 7 curated docs; `design/SKILL.md` is included as a regression guard. See [Named plans](Named-Plans). | [`scripts/check-multi-plan-naming.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-multi-plan-naming.sh) |
| check-worktree-slug | The worktree slug-safety invariant (V5-10 / LC-2): a worker in a `git worktree` can't see the parent's gitignored `.harness/`, so it resolves the vault slug by the **origin basename** alone (Tier 3). If the full-chain slug (an explicit `vault_project` / `github.repo` override) diverges from the origin basename, a worktree worker would silently write plans/progress under the wrong `projects/<slug>/`. Delegates to `vault_project.py check-worktree-slug` (the same resolver the `doctor` probe calls, so gate + probe never drift); no origin remote → warn-only. | [`scripts/check-worktree-slug.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-worktree-slug.sh) |
| check-no-auto-worktree | No agentm automation surface auto-spawns a worktree (V5-10 / LC-3): worktrees are an **operator-initiated** primitive (the spawn helper lives crickets-side). Scans executable surfaces (shell · python · pwsh · CI yaml) for the `git worktree add` spawn verb; read/cleanup subcommands (`list`/`remove`/`prune`) are allowed, tests + this gate's own file excluded. Proves agentm itself never creates a worktree unprompted. | [`scripts/check-no-auto-worktree.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-no-auto-worktree.sh) |
| check-one-way-imports | One config-driven gate over five one-way import-direction invariants (CONS-1 merge of what were three separate scripts, converged onto Python — `opinion-resolver`, `capability-resolver`, `process-seam`, `lc8-storage-vault`, `lc8-bridge` rules): (1) **opinion-resolver** — `opinion_resolver.py` never imports plugin code; (2) **capability-resolver** — `capability_resolver.py`/`capability_version_match.py` are one-directional (V5-8 Task 4), reading plugin manifests as data, never importing plugin code at runtime; (3) **process-seam** — the memory↔process edge is one-directional (V5-4 / LC-4): the process-seam client (`scripts/process_seam.py`) imports the memory engine, never the reverse — scans agentm's Python automation surfaces for any module importing `process_seam` (AST module-name equality, not a regex — a same-prefix sibling like `process_seam_helper` can never be mistaken for the real module), excluding `test_*.py` and `process_seam.py` itself; (4) **lc8-storage-vault** — the de-vaulted routing files (`harness_memory.py`, `repo_registry.py`) never import the `storage_vault` capability plugin directly (LC-8); (5) **lc8-bridge** — kernel toolkit scripts under `harness/skills/memory/scripts/` never import `harness_memory` back (V5-5 LC-8 bridge extension). Runs all five by default; `--rule <name>` runs just one. Runs cross-OS via the auto-discovered `unit tests` step (subprocess tests in `test_process_seam.py`, `test_check_opinion_resolver_one_way.py`, `test_check_capability_resolver_one_way.py`), so no separate per-OS workflow step exists. See [Memory↔process seam](Memory-Process-Seam), [Process seam](Process-Seam), and [Orchestration bridge](Orchestration-Bridge). | [`scripts/check-one-way-imports.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-one-way-imports.py) |
| check-storage-seam-no-path-leak | No `pathlib.Path` crosses the memory↔storage seam (V5-1): every seam verb returns the seam's own `Locator`/`Info`, never a path, so a filesystem assumption can't reach the engine. *Static* (AST, not grep): parses each `scripts/storage_*.py` and flags any seam verb (`resolve`/`read`/`write`/`list`/`exists`/`info`/`mkdir`) whose **return annotation** references a path type, however nested (`Path`, `list[Path]`, `Path | None`, `os.PathLike`). Internal `Path` use is fine — a filesystem backend's `root / key`; only handing one back is the leak. `test_*.py` is out of the glob, so conformance fixtures (which build a `Path`-returning backend to test the gate) don't trip it. The structural sibling of `check-one-way-imports`'s `process-seam` rule; runs cross-OS via the auto-discovered `unit tests` step (subprocess tests in `test_storage_seam.py`). See [Memory↔storage seam](Storage-Seam-Concepts) and [Storage seam](Storage-Seam). | [`scripts/check-storage-seam-no-path-leak.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-storage-seam-no-path-leak.py) |
| check-opinion-honesty | No orphan `Opinion` references (agentm-opinion-registry.md Enforcement §3): every consumer-declared Opinion name must resolve to a real `opinions/<name>.md` entry. Today the only consumer-declared field this checks is a persona's `opinions:` manifest key, which is itself `[PENDING-IMPL]` — so the gate is almost always a quiet no-op until personas start declaring opinions, which is correct, not a bug. No direct CI step of its own — runs via the `UNIT_WRAPPED` allowlist (`test_check_opinion_honesty.py` subprocess-invokes it), same pattern as `check-ci-consistency`. | [`scripts/check-opinion-honesty.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-opinion-honesty.py) |
| check-personas | Every `kind: persona` manifest under `personas/` keeps `requires: ⊆` substrate-native scripts (no hard dependency on a crickets capability) and never sets `always_load`/`always-load` to `true` (both spellings checked) — see [persona-tier-schema reference](persona-tier-schema). | [`scripts/check-personas.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-personas.py) |
| check-governs-index | Every `governs:`/`area:` stamp in `wiki/designs/` resolves cleanly against `governs_resolver.build_index()`: no two designs claim the same `governs:` pattern (overlap), and every `area:` value is in the canonical AG taxonomy (unknown-area) — catches an authoring mistake at commit time instead of a grounding hook silently treating the file as greenfield (R0.10). | [`scripts/check-governs-index.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-governs-index.py) |
| check-no-pii | The regex PII scanner finds no personal info across the tree (this is a public repo). | [`scripts/check-no-pii.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-no-pii.sh) |
| check-retrieval-regression | The shipped ranker has not regressed against a pinned baseline on the frozen gold set. Its measurement contract — resolution, provenance, pre-registration rules — is [Retrieval-Eval-Contract](Retrieval-Eval-Contract). It runs [`eval_retrieval_shipped.py`](https://github.com/alexherrero/agentm/blob/main/scripts/health/eval_retrieval_shipped.py), which issues each query exactly as the recall hook does — the same ×2 over-fetch, `_daemon_admissible` filtering, and temporal bounds — against `agentmd search`, the ranker anyone actually queries, and compares per-question hit/miss against `scripts/health/fixtures/week1-gold/shipped-baseline.json` with an exact paired test. Three states rather than two: **PASS**, **FAIL**, and **SKIP** when there is no pinned baseline or the corpus is cold. The skip is deliberate and narrow: the eval refuses to report on a cold embedder or above 5% stale vectors, because a lexical-only run reported as a hybrid number is worse than no number. A FAIL now has two distinct causes: the ranking regressed, or the comparison was refused (`COMPARISON REFUSED`, exit 3) because the baseline carries no corpus fingerprint (documents, embedded-in-scope count, gold-set hash, pin date) or names a different gold set — the gold-set check is never overridable.734. This script always passes `--drifted-ok`: it is the standing tripwire against the live vault, so ordinary corpus movement prints beside the verdict instead of refusing. This gate exists because the eval it replaces had sat behind a promotion criterion nothing read — anything shipped shadow-first here names its reader in the same change. Since filing v2 part 3 the gold set's pinned pre-migration paths are translated, note by note, through the migration's own disposition reports (`<memory root>/diagnostics/migrations/corpus-migration-3/*/dispositions.csv`, later runs overriding earlier) and the operator's whole-tree moves are exact prefix remaps beside 2b's; a path no report moved compares as it is, and a machine without reports runs the pre-migration comparison. A basename fold was tried first and rejected in the part-3 pre-tag review — it would have called two notes that shared a name in different month buckets the same note. Against the migrated corpus the gate reads 0.734 → 0.688 (one flip to a hit, four to a miss, exact paired p = 0.375, no significant regression); two of the misses are the primos questions, whose records moved to `Projects/primos/` and carry the ranker's project-space penalty. | [`scripts/check-retrieval-regression.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/check-retrieval-regression.sh) |
| check-storage-rules | The filing contract parses, and the taxonomy has not grown without a warrant. `standards/storage-rules.md` steers filing at runtime, which makes a typo in it the newest silent-failure surface in the system — a model handed a malformed rule does not stop, it improvises, and the filing that results looks fine and is wrong. Two assertions. **parse**: the shipped contract embedded in the daemon parses and validates, always; the live vault instance does too when a vault resolves. A vault with no rules file is a documented skip, because absence falls through to the embedded copy by design; a vault whose rules file is corrupt is a failure. **growth rule**: a diff that adds a value to `memory_types` carries its warrant in the same diff — the query class that needs it, the nearest existing type, and why that one does not fit — machine-checked against the block's own `warrants` map. Fifty-odd values accumulated in the old taxonomy because every addition was individually defensible and nothing ever asked whether the set still cohered; this is the brake that asks. Unlike [check-kind-taxonomy](#advisory-checks), which is report-only because the live corpus has known data-quality problems a hard gate would block unrelated work on, **this gate fails the battery**: it guards one file, and it is the file everything reads. The parse itself is done by the daemon (`agentmd rules`), which is the only parser of that file — see [Memory daemon § the filing contract](Memory-Daemon#the-filing-contract). | [`scripts/check-storage-rules.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-storage-rules.py) |
| check-vocabulary-membership | No note enters the corpus carrying an unregistered `type:` or `kind:` value — the enforcement gap the [Filing v2](agentm-filing-v2) census found: the type-XOR-kind exclusivity rule held at zero violations while `kind:` alone had drifted to 38 in-use values against a 32-value register, because only the exclusivity was ever checked, never the values themselves. Two rules close it. A **collision** check inside the daemon's own parser fires on every parse and refuses any word claimed by both `memory_types` and `record_kinds`. A **membership** audit over the live corpus applies **set-ratchet semantics**: a recorded baseline stores every currently-tolerated offender as a `(path, value)` pair, a swap can't hide inside a stable count, any offender missing from the baseline fails the battery immediately, and the baseline only ever shrinks. Retired values (named in the contract's own `deprecations` map) sit in a separate migration-pending bucket, outside the offender count. `--strict` drops the baseline and fails on any offender at all — the mode `check-all.sh`'s invocation now runs, the corpus migration (filing v2 part 3) having drained the baseline to empty; the live strict run reads 0 offenders over 2,291 notes. **Two halves, two places**: a CI runner has no vault, so `tests-linux.yml` runs `--self-test` only — the collision fixture the parser must refuse, and a scratch-vault unregistered value the audit must catch; the corpus ratchet itself runs locally through `check-all.sh`, against `MEMORY_VAULT_PATH`, where the real corpus is. The live first run recorded a zero-offender baseline over 212 retired-value notes awaiting the migration. | [`scripts/check-vocabulary-membership.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-vocabulary-membership.py) |
| check-vault-frontmatter | Every note in the vault has frontmatter that parses as a YAML mapping. Three classes: **parse-error** — `yaml.safe_load` raises, usually on an unquoted scalar holding a colon-space, which YAML reads as a nested mapping and rejects; **not-a-mapping** — the block parses, but to a list, a bare scalar, or nothing; **truncated-value** — an unquoted scalar holds a ` #`, so YAML ends the value at the comment and drops the rest without raising. Nothing else in the stack parses frontmatter strictly: `frontmatter_validator.py` and `vault_lint.py` are stdlib-only by design and split `key: value` on the first colon, and the Go daemon's `splitFrontmatter` is a regex split. This gate deliberately does **not** inherit the `_EXCLUDE_DIRS` frozenset both linters carry, because `_harness/` sits in it and is where the design and handoff memos live — the notes most likely to break, since their `status:` and `inputs:` fields carry long prose. Nine notes carried unparseable frontmatter for up to two months under green CI before the gate existed; the first run of the gate found two more, both silent truncations under `_harness/`, one of them losing 213 of 261 characters. A `#` that opens a conventional comment (a space follows it, or it ends the line) is left alone, and every truncation is confirmed against the parser before it is reported — the scanner predicts the prefix YAML would keep and reports only when `safe_load` returned exactly that. **Two modes, because a CI runner has no vault:** `check-all.sh` runs the scan over the resolved vault (`harness_memory.vault_path()`, graceful-skip when nothing resolves), while CI runs `--self-test`, which builds a scratch vault of known-bad notes and asserts each defect at the expected `file:line`. `test_check_vault_frontmatter.py` drives the scanner against scratch vaults on all three operating systems via the auto-discovered unit-test step. | [`scripts/check-vault-frontmatter.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-vault-frontmatter.py) |
| check-memory-root-consistency | The two roots must agree about where memory lives. Two invariants, because one alone missed a real failure: (1) every `daemon.spaces` value sits at or beneath `memory_root`; (2) where the Go daemon (`daemon.spaces`) and the Python stack (`plugins.obsidian-vault.spaces`, relative to `memory_root`) both name the same space, they must resolve to the same directory — containment alone isn't agreement. Absent keys are not failures: an unset `memory_root` defaults to the vault root, so every space is trivially beneath it (the pre-cutover topology), and a space the Python side doesn't name falls through to its own default rather than being compared. **One designed exception** (filing-v2 part 2b): `daemon.spaces["projects"] = "Projects"` names the vault-root sibling deliberately outside `memory_root`, so containment is waived for exactly that value — agreement still holds, checked against the Python side naming the same sibling as `../Projects` (explicit or its default). `test_memory_root.py` covers the base invariants; `test_projects_merge_layouts.py` pins the sibling exception. | [`scripts/check-memory-root-consistency.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-memory-root-consistency.py) |
| check-no-hardcoded-vault-path | No tracked non-test file contains a hardcoded absolute vault path: (A) `…/Library/CloudStorage/…` literals (not shell tilde/variable expansions); (B) the retired pre-V5-3 vault root name as a path component (`…/Obsidian/AgentMemory`). Ensures vault-path hygiene survives refactors — callers must resolve the live path at runtime, not bake a machine-specific or stale-root path into source. | [`scripts/check-no-hardcoded-vault-path.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-no-hardcoded-vault-path.py) |
| check-registry-hygiene | No `root_path` in the live repo registry (`<memory_root>/_meta/repos.json`) sits under a temp root — `$TMPDIR`, `/tmp`, `/var/folders`, or their `realpath` forms. A test that builds a fixture repo in a temp directory and registers it leaves an entry whose path died with the test; three accumulated by 2026-08-10 (`novault-marker`, `redetect-cli`, `redetect-demo`) because the tests that made them popped `$MEMORY_VAULT_PATH` to simulate "no vault" without redirecting `$AGENTM_INSTALL_PREFIX`, so `vault_path()` fell through to the operator's real config. The cost is not the junk rows: they re-appear on every run, so `repos.json` is permanently modified-but-uncommitted. Until 2026-08-10 the daemon committed markdown only, so nothing cleared it and `agentmd gate corpus-write` stayed shut; the committer now commits what git reports, which closes that half, and this gate closes the other by keeping the churn from arriving at all. A gate held closed by unrelated churn gets worked around, and then it protects nothing. Tests should use `no_vault_configured()` in `scripts/test_project_config.py`, which redirects both variables; this gate is the tripwire for the ones that don't. Graceful-skips when no vault resolves. | [`scripts/check-registry-hygiene.py`](https://github.com/alexherrero/agentm/blob/main/scripts/check-registry-hygiene.py) |
| verify-v4 | Kernel-owned auto-orchestration contracts: config seed/parse (A), idle-chain dry-run ordering + bounded execution (E), emit gating (shifted-guard + cooldown) + atomic state write + single-writer invariant (G static) — against a throwaway scratch vault. Linux/Mac only. | [`scripts/verify-v4.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-v4.sh) |
| verify-orchestration-briefing | PM-half of the orchestration push surface (V5-5 / LC-9): briefing signals (inbox · HIGH watchlist · incubator · idea-ledger · staged-adapt · both nudges) · staged-adapt surfaces-and-clears. 10 checks against a throwaway scratch vault. Runs as a direct step in `tests-linux.yml`/`tests-mac.yml` (previously check-all.sh-only and invisible to CI; testInfra#1). Linux/Mac only. Session-marker and phase-dispatch scenarios are in `verify-phases`. | [`scripts/verify-orchestration-briefing.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-orchestration-briefing.sh) |
| verify-idle-chain | The non-dry-run counterpart to `verify-v4`'s segment E (issue #71, Hardening I #45 follow-up). Runs the idle chain and a `post-release` dispatch **for real** against a scratch vault and scratch transcript root, asserting the mutations rather than the plan: the mined preference that lands, the seen-state that advances, the candidates that stage, the cooldown that is recorded and then honored, the skill-pointer entry the dispatch writes. Hermetic — no network (comments-only discovery whitelist, seeded cadence state, no GitHub links in the adapt fixture), no real vault, no real transcripts. `VERIFY_IDLE_CHAIN_FAULT=dry-run` re-runs segment A with `--dry-run` so every mutation assertion fails, proving the gate catches a chain that plans and never does. Manual/local only; CI runs the plain invocation. Linux only. | [`scripts/verify-idle-chain.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-idle-chain.sh) |
| verify-hook-resolution | Regression guard for the hook-config dual-key `vault_path` read (R1.2 / agentmEngine#0): builds a scratch HOME with a `.agentm-config.json` carrying ONLY the plugin-namespaced key (`plugins.obsidian-vault.vault_path`) and a scratch vault, then runs all four memory hooks (session-start, prompt-submit, reflect-stop, reflect-idle) against it and asserts each one actually resolved the vault. `VERIFY_HOOK_RESOLUTION_FAULT=1` strips the vault key entirely so the same assertions fail loudly, proving the fault-injection mode has teeth (mirrors the `VERIFY_MEMORY_FAULT` convention in `verify-memory-roundtrip.sh`). Runs as a direct step in `tests-linux.yml`/`tests-mac.yml`. Linux/Mac only. | [`scripts/verify-hook-resolution.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-hook-resolution.sh) |
| verify-state-routing | Regression guard for the backend × project-mode routing matrix (R1.3 / agentmEngine#1) plus the never-demote invariant: (A) `storage.backend` resolving to `vault` routes state to `<vault>/projects/<slug>/_harness/`, never the repo-local `.harness/`; (B) `.harness/.project-mode=local` opts out even with a vault configured; (C) no backend configured falls back to device-local `.harness/`; (D, unconditional) `storage.backend=vault` explicitly configured with a broken/missing vault directory makes `write-state` exit non-zero and never silently write to the device-local fallback — the never-demote guard from [Storage seam § the fail-loud guard](Storage-Seam#the-fail-loud-guard). `VERIFY_STATE_ROUTING_FAULT=1` reproduces the historical pre-fix bare-`except Exception` shape inline (a local shadow, never patching production code) and asserts it *would* have demoted, proving the fixture is faithful to the original bug. Writing this check surfaced a real module-identity bug in `harness_memory.py`, fixed in the same change — see [Storage seam § the fail-loud guard](Storage-Seam#the-fail-loud-guard) for the mechanism. Runs as a direct step in `tests-linux.yml`/`tests-mac.yml`. Linux/Mac only. | [`scripts/verify-state-routing.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-state-routing.sh) |
| verify-reflection | Regression guard for two independent invariants of the reflection pipeline (R1.5 / agentmExperience#2, the R0.3 source-filter fix): (1) **classification**, mode-independent — calls `reflect.mine_transcript()` directly; a genuine operator preference statement ("I prefer kebab-case slugs...") classifies HIGH, while a junk corpus of bare always/never phrasing ("the deploy job always crashes...") never classifies HIGH (R0.3 demoted the bare always/never pattern out of the HIGH lane, since it fires on ordinary discussion just as readily as on operator directives); (2) **source filtering** — runs the real `reflect.py corpus` CLI across all three `--route-mode` values (auto/silent/interactive); a transcript under a `subagents/` path, carrying the same still-HIGH preference pattern repeated 3x, never contributes anything to the vault in any mode, because `_discover_transcripts` excludes `subagents/` paths before `mine_transcript` ever runs on them. `VERIFY_REFLECTION_FAULT=1` calls `mine_transcript()` directly on the machine-source fixture (bypassing `_discover_transcripts` entirely) and asserts it WOULD classify HIGH, proving the fixture is a faithful trigger for the historical junk-slug pollution bug the filter exists to prevent — manual/local-only (CI runs only the plain invocation). Runs as a direct step in `tests-linux.yml`/`tests-mac.yml`. Linux/Mac only. | [`scripts/verify-reflection.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-reflection.sh) |
| verify-mcp-surface | Regression guard for the MCP memory-tool surface (R1.6 / agentmEngine#2): an in-process FastMCP client (`FastMCPTransport`, no HTTP daemon) drives the real server object from `memory_mcp_server.py` against a scratch vault. Covers (A) the `memory_append` → `memory_search` → `memory_forget` round-trip; (B) soft-delete semantics — a forgotten entry stays at its original path with `status: deleted` in frontmatter (never unlinked, never moved to `_archive/`), excluded from `memory_search` by default, surfaced again with `include_deleted=True`; (C) the three dead-recall-surface fates — `memory_recall` retired (absent from `tools/list`, raises `ToolError` on call), `process_seam.recall_here` retired (checked via subprocess, not a top-level import, so the check itself doesn't trip `check-one-way-imports`'s `process-seam` rule), and `harness_memory.documenter_context()`/`phase_recall()` confirmed as the permanent V5-3 stub (neither wired nor retired). `VERIFY_MCP_SURFACE_FAULT=1` deletes the scratch vault between append and search and asserts `memory_search` raises rather than returning stale/empty results. Requires the `fastmcp` package — gracefully skips (exit 0, SKIP) if it isn't installed, matching `test_memory_mcp_tools.py`'s existing convention. Runs as a direct step in `tests-linux.yml`/`tests-mac.yml`, right after `verify-reflection`. Linux/Mac only. | [`scripts/verify-mcp-surface.py`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-mcp-surface.py) |
| health-score-determinism | The health-scorecard aggregator (`scripts/health/health_score.py`, stdlib-only) reads JSONL check records from stdin or `--path` and groups them by `axis` — one of eight locked family names, each with a fixed weight: memory persist+recall 25, plan-adherence+drift 15, verification honesty 15, capability function 15, memory freshness+experience 10, efficiency 10, docs+voice health 5, safety/recoverability 5. Each axis/family score is 100 × sum(weight for passing checks)/sum(weight), excluding `{"pass": null, "dark": true}` dark (designed-not-built) records from both numerator and denominator; the Health Index is the weighted mean across only families with at least one non-dark record, so an untested family doesn't drag the index down. `--check-determinism` runs the aggregator twice against the same fixture input and asserts byte-identical markdown output. Task 1 of 5 in the R1 dashboard plan (R1.8). Task 2 wired real JSONL emission into all nine `verify-*` suites (`--jsonl-out`/`$HEALTH_JSONL_OUT`, additive-only — see [`scripts/health/README.md`](https://github.com/alexherrero/agentm/blob/main/scripts/health/README.md) for the pipeline and the per-suite axis mapping); this row still covers only the aggregator's own determinism, not suite-level JSONL correctness. | [`scripts/health/health_score.py`](https://github.com/alexherrero/agentm/blob/main/scripts/health/health_score.py) |
| validate-audit-coverage | Task 3 of the R1 dashboard plan (R1.8): proves the regression net actually catches what the mythos-readiness audit caught by hand. The audit ledger names 11 verified blockers; this gate covers 4 — the ones with real code presence in this repo (agentmEngine#0, agentmEngine#1, agTrack#0, voice#0); agentmExperience#0 (a dead vec-index drain) was a fifth until the vector stack was removed and its subsystem stopped existing — and explicitly reports the other 6 as out of reach for an agentm-repo verify script rather than silently omitting them. Of those 6, 3 (cricketsPluginsA#0, cricketsPluginsA#1, cricketsPluginsB#0 — the `find_capability` fallback chain, the `finalize_unit` PII guard fail-open, and the pricing re-pin) now have dashboard visibility from the other side of the seam: crickets' [`scripts/health/run-crickets-fast-tier.sh`](https://github.com/alexherrero/crickets/blob/main/scripts/health/run-crickets-fast-tier.sh) (added PLAN-r2-ledger-and-dist task 7) emits a `capability function` check record per blocker via the new shared `crickets/scripts/health/jsonl_emit.py` helper, consumable cross-repo the same way this script's own records are. The residual genuinely-out-of-scope count is 3 — roadmapMaster#0, wikiAgentm#0, wikiCrickets#0, all documentation/content staleness with no fault-injection concept. Operator-confirmed scope narrowing, 2026-07-03; cross-repo dashboard-visibility update, 2026-07-05 — see the script's own header comment for the full per-blocker breakdown and detection mechanism. | [`scripts/health/validate-audit-coverage.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/health/validate-audit-coverage.sh) |
| verify-dreaming | End-to-end regression guard for the dreaming pipeline (AG Wave E) and the health-scorecard's live signal for the `memory freshness+experience` axis's dreaming row — drives the real `dream.py` / `dream_confirm.py` / `revert_log.py` CLIs and the `templates/jobs/dream.yaml` manifest against a scratch vault, proving the stage→confirm→revert-log wiring end-to-end (unit-level coverage of each stage's own logic lives in `test_dream*.py` / `test_revert_log.py`; this script proves the wiring, not every branch) and that a timed-out staged dream never silently applies. | [`scripts/verify-dreaming.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-dreaming.sh) |
| verify-auto-org-meters | End-to-end regression guard for auto-organization's two vault-health meters (`PLAN-auto-org-dedup-and-lint.md` task 8) and the health-scorecard's live signal for the `memory freshness+experience` axis's meters row — drives the real `dream.run_dream()` against a scratch vault seeded with a linked pair, an unlinked note, a shelved artifact, and an archived memory, asserting the organic-connectivity ratio and the three-state (live/shelved/archived) browse-surface count both land in `corpus_stats` and render in the digest, and that the archived note's content stays readable on disk — the plan's own acceptance test, "browsing agentm's memory shows live, current notes, and aged material sits in the archive, still there on request." Unit-level coverage of each meter's own logic lives in `test_dream.py`'s `ConnectivityMeterTests`/`BrowseSurfaceCountsTests`; this script proves the two meters' wiring through a real cycle, not every branch. | [`scripts/verify-auto-org-meters.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-auto-org-meters.sh) |
| verify-phases | A full phase lifecycle (`/setup → /plan → /work → /release`) drives its deterministic seams — state read/write, `progress.md` appends, `features.json` updates, post-phase dispatch plumbing — end-to-end on a throwaway fixture project, run twice: once vault-resident, once repo-local. Also covers session-marker scenarios (no-session / single-marker / ambiguous concurrency-safe) and post-release discover-skills chain (V5-5 / LC-9). Linux/Mac only. | [`scripts/verify-phases.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-phases.sh) |
| run-ablation-baseline | For each of the four R3.1a subsystems (vectors, reflection, gates, evidence-tracker), runs its `verify-*` script twice — once on, once with its `ABLATE_*=1` off-switch — scores each run's check records through `health_score.py`'s own axis-scoring path, and emits one ablation record per subsystem (`{subsystem, axis, score_on, score_off, uplift}`). Deterministic, fixture-driven, no live model calls. The uplift magnitude is advisory reading only — the gate's own exit code is 0 unless a subsystem's on/off pair can't be scored at all (a setup failure, not a low uplift number). | [`scripts/health/run-ablation-baseline.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/health/run-ablation-baseline.sh) |
| verify-memory-roundtrip | The memory engine round-trips on a throwaway fixture vault: stub-mode `embed` (deterministic hash vector, no network/model) → `save` → `recall query` surfaces it by content → `reflect` a synthetic transcript → `vault_lint` clean. A `VERIFY_MEMORY_FAULT=drop-save` injection drives the negative path. The index-build and nearest-neighbor sub-checks went with the vector stack ([week-1 experiment](agentm-rescope-week1-experiment)); `embed` is still round-tripped because `notes_link_discovery --embeddings` still uses it. Hermetic, Linux/Mac only. | [`scripts/verify-memory-roundtrip.sh`](https://github.com/alexherrero/agentm/blob/main/scripts/verify-memory-roundtrip.sh) |
| dogfood-workflows | Every workflow the harness ships as a template under `templates/.github/workflows/` is active at the repo root, byte-identical to the template. Mirrored locally by `check-vendored-parity`'s `workflow` mode (above), so a one-sided edit is caught in `check-all.sh` before the push, not as a red Linux run after it. | Inline job in [`tests-linux.yml`](https://github.com/alexherrero/agentm/blob/main/.github/workflows/tests-linux.yml) |
| check-ci-consistency | The both-places rule (testInfra#3): every `gate "<name>" <command...>` line in `check-all.sh` also runs in CI — either as a direct `tests-linux.yml` step, or via a documented `UNIT_WRAPPED` allowlist (see `scripts/test_ci_consistency.py` for the current entries — the count itself isn't restated here, since a hardcoded number just drifts) whose wrapper `test_*.py` genuinely subprocess-invokes the gate by name. Also checks gate-flag parity (`--strict`/`--all`) between `check-all.sh` and `tests-linux.yml`, and that `ci-all.yml`'s `WORKFLOWS` list points at real workflow files. Runs via the auto-discovered `unit tests` step, same pattern as `check-vendored-parity`'s `workflow` mode — no separate CI step of its own. | [`scripts/test_ci_consistency.py`](https://github.com/alexherrero/agentm/blob/main/scripts/test_ci_consistency.py) |

## `verify-v4.sh` — kernel-contracts integration check

This script checks the V4 #23 kernel-owned auto-orchestration contracts. It runs the real scripts via their CLIs against a throwaway scratch vault and asserts deterministic outputs. It acts as the integration complement to the per-function unit suite. We trimmed it to 85 lines at V5-5 / LC-9 when we extracted the PM-half and Developer-half.

| Property | Detail |
|---|---|
| Isolation | A `mktemp -d` scratch vault + an exported `IDEAS_SURFACE_PATH` — never reads or writes a real vault. |
| No side effects | No network, no transcript mining, no sub-agent dispatch; self-cleans via a `trap`. |
| Coverage | **Segment A:** config seed/parse. **Segment E:** idle-chain dry-run (ordering + bounded `--max-batches`/`--limit`). **Segment G:** emit gating (shifted-guard + cooldown) · atomic state write · static single-writer check: greps `orchestration_*.py` for `^def save_state` and asserts 0 matches — making the V5-5 LC-2 single-writer invariant a gate-checked property. |
| Out of scope | Briefing signals, staged-adapt, nudge triggers, phase-dispatch, and session-marker scenarios — those moved to `verify-orchestration-briefing.sh` (PM-half) and `verify-phases.sh` (Developer-half). |
| Extend it | Add a `check_*` per new kernel-owned contract (drive the real script against `$SV`; assert with `assert_contains`/`assert_equals`/`assert_absent`), keeping each check scratch-isolated. |

## `verify-orchestration-briefing.sh` — PM-half integration check

This script checks the PM-owned orchestration signals extracted from `verify-v4.sh` at V5-5 / LC-9. It will travel to the crickets PM-trigger plan when that ships. It runs agentm-side until then.

| Property | Detail |
|---|---|
| Isolation | A `mktemp -d` scratch vault — never reads or writes a real vault. |
| No side effects | No network, no transcript mining, no sub-agent dispatch; self-cleans via a `trap`. |
| Coverage | Every briefing signal: inbox · HIGH watchlist · incubator · idea-ledger · staged-adapt · both nudges (idea-promotion + watchlist-authoring) · staged-adapt surfaces-and-clears · phase-dispatch plans + session-marker resolution (no-session / single-marker / ambiguous concurrency-safe). 10 checks. |
| Out of scope | Kernel contracts (config seed, idle chain, emit gating) — those remain in `verify-v4.sh`. |
| Extend it | Add a `check_*` per new PM-half signal, keeping each check scratch-isolated. |

## `verify-idle-chain.sh` — the chain actually does something

`verify-v4.sh` segment E only asserted the idle chain's **dry-run plan**. That tests planning order and leaves every step free to no-op silently — the V4 #39 shape. This script closes the gap (issue #71): it runs the same chain **without** `--dry-run` and asserts what changed on disk.

Every assertion pins to observed reality — a file that exists, a literal from the fixture, a count from `find`. None recompute an expectation with the implementation's own formula, because a check that derives its expectation from the code under test only proves the two agree.

| Property | Detail |
|---|---|
| Isolation | One `mktemp -d` holds a scratch vault and a scratch transcript root, removed via a `trap`. |
| No network | The discovery whitelist is seeded comments-only and the cadence state is seeded fresh, so `discover_skills.py --cadence-check` returns before it fetches. Both are seeded, so a machine whose clock makes cadence miss still stays offline. The adapt fixture carries no GitHub links, keeping Pass-1 enrichment away from `api.github.com`. |
| No real transcripts | `MEMORY_TRANSCRIPT_ROOT` points at the fixture tree. The chain invokes `reflect.py corpus` **without** `--projects-root`, so that env var is the only thing standing between this gate and your real `~/.claude/projects`. Segment A asserts the seen-state names exactly the fixture session, which is what catches that plumbing breaking. |
| Coverage | **A:** the real chain — the mined preference lands in the vault, the seen-state advances, both candidates stage under the names the fixture headings imply, the reported `staged_candidates` matches the files on disk, `last_fire` is recorded. **B:** a second real run is a cooldown no-op that stages nothing new. **C:** `enable_idle_chain = false` mines and stages nothing. **D:** a discover step that did not throttle is never labelled `throttled`. **E:** a real `post-release` dispatch writes the skill-pointer entry. |
| Negative check | `VERIFY_IDLE_CHAIN_FAULT=dry-run` runs segment A with `--dry-run` — the regression this gate exists to catch — and every segment-A mutation assertion fails. Manual/local only; CI runs the plain invocation. |
| Found in the writing | Segment D guards a bug this gate surfaced. The discover-skills label grepped stdout for `cadence` and `skip`, which matched the `"cadence_skipped"` **key**, so every run reported `throttled` — including one that fetched all four network sources. The label now reads the flag. |
| Extend it | Add a segment per new chain behavior. Assert the mutation, never the plan. |

## Reading red CI

```bash
gh run list --workflow "[T] Linux Tests"    --limit 3
gh run list --workflow "[T] Mac Tests"       --limit 3
gh run list --workflow "[T] Windows Tests"   --limit 3
gh run view <run-id> --log-failed             # drill into the failing step
```

A red run on Windows but a green run on POSIX indicates a path-separator or pwsh-host assumption regression. A red run across all operating systems indicates a canonical-spec or adapter-parity drift. Try running `bash scripts/check-parity.sh` locally to verify.

### A run that failed without running anything

Not every red run is a failure. A run can be marked `failure` having dispatched
no work at all, and it reads exactly like a broken build until you look closer.

**The signature.** The run completed within a few seconds of being created, and
its jobs sit at `status: queued` with an empty `steps` list — GitHub accepted the
run and never assigned it a runner. A genuine failure has a job with
`conclusion: failure` and a named failing step.

```bash
# created and updated seconds apart => suspect an orphan, not a failure
gh api repos/<owner>/<repo>/actions/runs/<run-id> \
  --jq '{created: .created_at, updated: .updated_at, attempt: .run_attempt}'

# zero failed jobs on a "failed" run confirms it
gh api repos/<owner>/<repo>/actions/runs/<run-id>/jobs \
  --jq '[.jobs[] | select(.conclusion=="failure")] | length'
```

**Why it blocks the PR.** Those undispatched jobs stay pinned to the pull
request as *pending forever*. They are not slow; nothing will ever report them.
The PR sits `BLOCKED` on checks that cannot complete, and `ci-all` fails
honestly, because from its side a run that never ran and a run that failed look
identical.

**Recovery, in order.**

1. `gh run rerun <run-id>` — but **verify it took**. It can no-op silently; the
   run should move to `run_attempt: 2`. A run still reporting `attempt=1` after a
   rerun did not restart.
2. If the rerun does not take, close and reopen the pull request. That fires
   fresh `pull_request` events and produces new run IDs, which is the reliable
   fix. Note it also **clears auto-merge**, so re-arm with `gh pr merge --auto`
   afterwards.
3. Re-run `ci-all` **last**, never alongside the OS workflows. It polls
   `tests-linux`/`tests-mac`/`tests-windows` and requires `success` from each, so
   starting it while they are queued makes it fail waiting on its own inputs.

**Reading the aftermath.** GitHub keeps orphaned runs in the commit's history,
so the PR page shows old red beside new green. Trust `gh pr checks` and
`mergeStateStatus` over the run list — and compare run IDs: the fresh set shares
a higher ID prefix than the abandoned one.

*Why there is no `concurrency:` block anywhere, and why adding one would not help
here:* the orphaned runs are the **current** head's, and `concurrency` only
cancels *superseded* runs. It would not have prevented any of this. It would also
turn every routine force-push into an `aggregate` failure, since a cancelled run
is not `success` and that is the only value the poller accepts. The `push:`
trigger is filtered to `branches: [main]`, so PR pushes do not double-fire
either — there is no duplication to collapse.

## The nightly health tier (advisory, never a merge gate)

The `[H] Health Nightly` workflow (`.github/workflows/health-nightly.yml`, R1.8 Task 4) runs daily at 06:00 UTC and on manual `workflow_dispatch`. It layers heavier checks on top of the fast tier this page documents. These heavier checks are too slow or dependency-heavy to run on every push. They include real-embedding recall (no stub mode), a live MCP daemon round-trip over real HTTP, a report of every `VERIFY_*_FAULT=1` mode's outcome, and a cold-install dogfood pass. The job renders the scorecard and uploads it (HTML and the raw JSONL records) as a build artifact named `health-scorecard-ubuntu` with a 14-day retention. It never commits or pushes. The V8 proving Lane S on 2026-07-13 retired the branch/PR/`workflow_dispatch`/`--admin` merge machinery this job used to run. The job acts purely as a clean-runner regression signal.

R1.8 Task 5 added a `health-nightly-macos` job (`macos-latest`). This job runs the same heavy tier as a conformance leg. It uploads its own JSONL output as a build artifact named `health-heavy-tier-macos` with a 14-day retention. Task 5 also folds the static `scripts/health/dark-checks.jsonl` registry (5 designed-not-built capabilities) into the render pipeline. The scorecard carries a "Dark checks" section that lists them separately from the scored families.

You do not read this CI artifact day to day. A local `health-pass` runner job (`templates/jobs/health-pass.yaml`, a daily `agentm-runner.sh` tick) appends to the vault-resolved history ledger and renders `~/.cache/agentm/telemetry/scorecard.html`. The `/console` and the morning brief link to this local file. See [Health scorecard](Health-Scorecard) for details on that half.

**AA4 (2026-07-08) — the ⑫ Hardening-II cold-install acceptance, per the B3 ruling in `ROADMAP-TAIL-ADJUDICATIONS.md`.** We added two more checks to the cold-install dogfood step rather than creating a separate job. (5) **dashboard honest-dark on a bare install** — an observability-console render over a rollup built from zero telemetry events, and a Health-Scorecard render over only the static dark-checks registry (zero live records). Neither check may fabricate a number when nothing has actually run yet. (6) **fan-out/budget gates fail CLOSED with no config** — a scratch `.harness/` with no `budget.yaml` must still throttle a fleet that reports large spend. It must not skip the ceiling check outright. As built today, check 5's observability-console half passes. Its Health-Scorecard half and check 6 both FAIL. The `render_markdown` function shows a fabricated `Health Index: 0.0/100` 🟢 headline on zero live records. The `runner/cycle.py` `_read_daily_ceiling` function returns `None` on a missing `budget.yaml`. This disables the ceiling gate rather than applying a safe default. We filed both as queued bugfix plans instead of fixing them inline. An acceptance pass reports findings; it doesn't fix them.

This workflow **never blocks a merge**. The fast tier above (`tests-linux.yml`/`tests-mac.yml`) serves as the only merge gate. The nightly run can go red without stopping development.

## Running the gate set locally

One command runs the deterministic battery. It executes unit tests, every `check-*` gate, the integration checks (`verify-v4`, `verify-orchestration-briefing`, `verify-idle-chain`, `verify-hook-resolution`, `verify-state-routing`, `verify-reflection`, `verify-dreaming`, `verify-auto-org-meters`, `verify-mcp-surface`, `verify-phases`, `verify-memory-roundtrip`), and `run-ablation-baseline`. It prints a PASS/FAIL table and exits non-zero on any failure. The `scripts/check-all.sh` script acts as the living source of truth for the exact gate count. A restated number here drifts as gates merge or land. Count the `gate "<name>" ...` lines in the script if you need the current figure:

```bash
bash scripts/check-all.sh
```

The script deliberately omits the heavier `smoke-install` and `gitleaks` checks. CI runs those slower external tools on every push. Run them directly if you need them: `bash scripts/smoke-install-bash.sh` (POSIX) or `pwsh -NoProfile -File scripts/smoke-install-pwsh.ps1` (Windows). The `check-all.sh` script acts as the maintained source of truth for the local battery. Add a `gate` line to it as the project grows.

## `probe-round-trip.sh` — a probe, deliberately not a gate

`scripts/probe-round-trip.sh` reads the memory system's end-to-end claim: save a
fact, come back in a fresh process, ask sideways, get it back. It captures
through the real `agentmd`, optionally runs one enrichment call, and asks three
questions that share no content word with the note, against twelve distractors,
with a control question that must not return the target.

It is not in `check-all.sh` and should not be added. It needs a running embedder,
and `--enrich` spends a model call. A gate with those dependencies is a gate
somebody turns off, and the battery has to stay something you can run on any
machine at any time.

Three properties are worth knowing before you read a result from it. The dense
arm is required — `fusion` is lexical only, asking sideways is the case the dense
arm exists for, and a run without an embedder exits **2** (cannot run) rather
than passing. Sideways is checked rather than asserted: a question reusing the
note's own words is refused, because that proves the index can find a string, not
that a memory can be recalled. And the rank the target achieves is reported but
not gated, since ranking belongs to the retrieval track rather than to this
check.

```bash
bash scripts/probe-round-trip.sh --embedder-url http://127.0.0.1:8901 --embed-model embeddinggemma-300M-Q8_0
```

Add `--enrich` for the whole arc. Exit codes: **0** every bar met, **1** a bar
missed, **2** could not run.

## Related

- [Releasing Conventions](https://github.com/alexherrero/crickets/wiki/Releasing-Conventions) — CI must be green before you invoke `ship-release`.
- [How to refresh an installed harness](Update-Installed-Harness) — what a re-run touches vs. leaves alone.
- [Vault write protocol](Vault-Write-Protocol) — the protocol the `check-vendored-parity` gate's `vault-lock` mode keeps byte-identical across its two copies.
- [Foundations HLD](agentm-foundations-hld) — the installer-boundary rule the gates enforce.
