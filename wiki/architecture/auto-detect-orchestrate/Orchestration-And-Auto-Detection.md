<!-- mode: index -->
# Orchestration and Auto-Detection

_How the harness auto-orchestrates the right phase wiring + context injection by detecting a project's shape — no manual configuration._

Pages for the Orchestration and Auto-Detection component go under `architecture/auto-detect-orchestrate/`; list them in this folder's `_Sidebar.md`.
