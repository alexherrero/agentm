---
title: AgentM Vault
status: final
visibility: published
kind: design
scope: arc
area: agentm/vault-taxonomy
author: alexherrero
contributors: []
created: 2026-09-06
updated: 2026-09-11
last_major_revision: 2026-09-07
prd:
project:
---

# AgentM Vault

> [!NOTE]
> **FINAL** — approved 2026-09-07 at the close of the vault-perfection series (seven sessions, 2026-09-06 to 2026-09-07). It succeeds [Filing v2](agentm-filing-v2.md), inherited unchanged unless a section says otherwise. Each session's decisions are its Detailed Design subsection; the build is fourteen plans, listed under Launch Plans. `launched` is set by `/release` when the last of them ships.

## Context

### Objective

The filing system is built: every part Filing v2 named has shipped, the corpus has migrated, and the daemon and the dreaming binary run it nightly. What has not happened is you sitting down with the result and shaping it until it is yours. That means a layout that reads the way you think, a card that holds what you would want it to hold, and machinery you can explain in a sentence. This design is that shaping, one decision at a time, written down with the reason and the alternative beside each. It refines what shipped; it does not rebuild it.

### Background

Filing v2 landed between v9.9.0 and v9.20.0 and a survey on 2026-09-06 read the result the way you would, folder by folder in Obsidian. The bones held: six classes, the contract in `standards/`, the lifecycle axis, the write path, the calendar register, the Go dreaming binary. Around them sat what the build had not reached — five empty `desk/` folders kept alive by Drive's icon files, engine state still inside `Agent/`, a nested `memory/memory/` written by the daemon's own probe, maps named `workflow-2.md` through `workflow-7.md`, a hand-kept `Home.md` eight weeks stale, and ten calendar reviews each saying nothing was recorded. The corpus told the larger story. Of 744 notes in the six classes, 251 are enriched tool tallies, 99 are truncated "User stated" fragments, 115 are one-line skill-discovery blurbs, 31 are mined opinion supplements, and 32 are `~dup` twins. The memory a person would keep is on the order of 200 to 250 notes, and only two `preference` notes exist that are not a fragment. The queue was not behind: 283 of the 356 unfiled notes had already been enriched and scored below the floor, and nothing owned that verdict.

The reference layout is your work vault, a sibling deployment of the same lineage, photographed the same evening and transcribed into the research bundle. It is lowercase throughout, and its agent half holds `desk`, `diagnostics` and `memory` with nothing loose. Its memory classes are a few dozen notes each, named by subject; every project carries the same skeleton; a task flows from `agent/desk/tasks/` into its project's `drafts/` when it closes; its scorecards are daily; its card is ten readable fields. Home already matches on the bones. Where the two differ, this design says which way home goes and why. Some differences are drift to correct. Some are deliberate, because home is a few long projects with many tasks each, and a personal space work does not have.

The series runs seven sessions, each taking one question and ending with a written decision here. You decide anything about how the vault looks and reads, anything touching `personal/` or `projects/` content, every threshold that demotes or forgets, every deletion, and every change to what a surface shows you. The agent decides mechanism, ordering and naming inside the code, and says so in the log so you can reverse it. Design sessions run on the strong model; the builds that follow hand off to `/work` sessions on the cheaper one, one plan per landing group, deployed the same day, migrations under quiesce.

## Design

### Overview

The vault becomes one lowercase tree that reads the same on both machines. `agent/` holds `archive/`, `diagnostics/` and `memory/` and nothing loose. `memory/` holds only the classes, each a few hundred notes at most once the residue is purged, each note named by its subject. A project carries the skeleton work proved — a charter, a tracker, decisions, designs, research, a map — and its tasks live inside it, one directory per task with a plan, a progress log and a tracker. `standards/` is the four-file always-load surface. The calendar is facet files under a year map. One root note maps the vault; `Ideas.md` is generated over your idea cards.

A memory card is what you would write: a title, a type, a one-line summary, the reason it was kept, an importance you read, the filing state, the lifecycle, one confidence tier, the transport it arrived by and the trust that implies, two dates, tags and relations — then a machine block the passes own. A card gets one deep pass, with analysis and research, and light passes afterwards, updated only when new input arrives. Your own spaces — `projects/` and `personal/` — are permanent: never demoted or archived by policy, ranked by importance rather than age, and enriched only in ways that leave the original content untouched. A card is written where the knowledge is: by the model in the room, the moment something durable settles, with the reason it was kept. What a writer cannot know it leaves empty, and an empty `why` is what marks a candidate for the nightly pass to judge.

### Infrastructure

No new component. The series changes the contract, the writers, the generators and the migrations that already run.

| component | role | what the series changes |
|---|---|---|
| `agentmd` | index, retrieval, MCP surface, capture, enrichment, the probe | field stamps, the card shape, ranking by importance in permanent spaces, the probe's root; the tool's four capture fields, one meaning for `status`, enrichment owed by stamp, the eager trigger removed, the tally gate; the batch reads the tier table, the two-shape prompt, usage printed (session 3); a hit is the card's readable head, the clock written for MCP and CLI hits, the ledger's `surface:` field, Claude Desktop as a client (session 6) |
| `agentmdream` | the nightly mutation pass, maps, rollups | map naming and pagination, empty-rollup skip, deep-once enrichment state; `-every 24h`, promote reads Captured and Candidates and writes semantic candidates, a report pass leaves the clock alone (session 3); the mocs job over the projects space and the weekly crystallize phase (session 4); the archive move and deletion inside the lifecycle job with a manifest first, the `retain`, `sequence` and `reconcile` jobs, the `completed/` moves, the activity signal on project trackers and the `dreaming` facet (session 5) |
| session hooks | recall at start and prompt; the trace and the miner at stop | the trace's shape; the miner files HIGH only and hands the rest to the trace (session 2, decided); `project:` and `task:` stamped from the binding and the twenty-line opening brief (session 4); the loader strips the contract's block under a 40,000-token gate, `standards/` leaves recall, traces are served on time questions only (session 6) |
| the pasted payload | claude.ai, the Gem, Antigravity | layout-free, pointing at `index.md`, `standards/` and `moc-projects.md`; local copies derived from the template; the email door as every chat surface's write path (session 6) |
| the runner | scheduled jobs | the parked health scorecard, a daily corpus scorecard, the nightly enrichment batch; the night window, the morning note, the Python cycle shrunk (session 3) |
| the gate battery | deterministic verification | naming, card-shape and class-directory gates |

Everything still runs on your Mac under launchd, triggered at session boundaries and nightly, with no server and no cloud component.

### Detailed Design

#### The layout (session 1, decided)

The tree, with the reason and the alternative beside each move.

```
<vault>/
├── agent/          archive/  diagnostics/  memory/  nothing loose · archive/ mirrors memory/ (session 5)
├── calendar/       YYYY/ facet notes · moc-calendar-YYYY.md · rollups only for periods with entries
├── personal/       yours; your Title Case filing inside, untouched by this series · ideas/ holds the idea cards (session 7)
├── projects/       completed/<slug>/ · <slug>/ charter.md tracker.md followups.md · decisions/ designs/ research/ drafts/ · tasks/NNN-<verb-slug>/ completed/ desk/ · moc-<slug>.md
├── standards/      storage-rules · user-preferences · security-and-secret-governance · moc-standards · voice/
├── index.md        the one root map
└── Ideas.md        generated over personal/ideas/, your preamble kept (session 7)
```

**The root spaces are lowercase.** `agent`, `calendar`, `personal`, `projects`, `standards`. Today four are Title Case and one is not, and the other tree you read every day is lowercase. Filing v2 ruled no top-level renames because renames churn; the churn is one landing group with the structural-move tooling that already exists, and a case-only rename on a case-insensitive disk goes through a temporary name. *Why not Title Case everywhere:* it would rename one folder instead of four, but every path in the loader, the contract and the daemon names `standards`, and it would keep the two vaults different for no reason you hold. *Re-audit* if Drive ever fails to follow a case-only rename; the two-step exists for that.

**`agent/` holds `diagnostics/`, `memory/` and, since session 5, `archive/`, nothing loose, nothing hidden.** `desk/` goes: its five folders are empty, scratch left the vault and projects moved out, and a task now lives inside its project. `_dream/`, `_meta/repos.json`, `.heat.json` and `.lifecycle.json` leave for the engine directory, which already holds the same layer's insights, staging and journals; the how-to twin in `_meta/` moves to `standards/`, where an operator-facing rule document belongs. `Home.md` retires (below). *Why not keep `desk/tasks/` for tasks without a project:* none exists today, and this vault's own rule is that a directory is discovered, never conjured; the day one appears, `desk/` returns with it. *Re-audit* when the first project-less task is filed.

**`memory/` holds only the classes.** The pen, `voice-kernel.md`, moves to `standards/`, where the loader already reads; the two watchlists and the three loose settings files move to `projects/agentm/`, because they are that repo's feature state and not memory; the nested `memory/memory/` is deleted once the probe writes at the memory root. *Why not the engine directory for the watchlists:* you edit those files in Obsidian, and the engine directory is not in the vault. *Re-audit* if a second feature grows working state that wants to live beside the classes — the answer is the same, its project.

**`crystallized/` is flat and holds only lessons with `consolidated_from`.** The six opinion lanes retire; they hold 31 mined fragments at `status: proposed`, and the class holds no lesson at all. Work's `crystallized/` is ten syntheses, one per system or task cluster, and that is the shape sessions 3 and 4 decided: a lesson is written only by the weekly crystallize phase, with `consolidated_from` naming every source. *Why not keep the lanes elsewhere:* the accumulate loop that read them retired with the inbox triage engine, and a feature with no reader is a directory with no purpose. *Re-audit* if the opinion feature returns with a reader; its data then lives under the feature's project.

**`entities/` folds until something writes it.** The directory has held only its index since 2026-08-19. The class stays in the contract, reserved the way `person` is reserved for email ingest, and the directory is recreated by its first writer. Work's `entities/` is populated and subfoldered because work ingests bugs, changes and chat threads; home ingests nothing yet. When it returns it is flat, per Filing v2's D7. *Why not keep the empty directory as a promise:* an empty shell is how the six-class layout failed once already. *Re-audit* when an ingest source lands.

**`episodic/` stays.** Session traces are its writer now; ingested threads and mail join when ingest arrives. What a trace holds is decided in session 2 (Asked · Outcome · Captured · Recalled · Candidates); when the survey ran it was a first prompt and a list of what recall injected.

**Maps are named for what they map, and `Home.md` retires.** `mocs/` holds `moc-root.md`, `moc-memory.md` and `needs-review.md`; a per-type page exists only past a member threshold, and a page that must paginate paginates inside itself, never into `workflow-2.md`. `moc-root.md` is the entry point, generated nightly; each area keeps its own `moc-<area>.md` beside its files, as diagnostics already does. *Why not a generated `Home.md`:* the map already exists under its own name, and a second entry point is a second thing to keep true. *Why not a hand-kept one:* it has been wrong for eight weeks and was the first link on every map. *Re-audit* if the root map grows past a screen; split by area then.

**`calendar/` is facet files under a year map.** `YYYY/YYYY-MM-DD-<facet>.md`, created only on a day with content for that facet, and `moc-calendar-YYYY.md` beside the year, generated. The bare-date day index Filing v2 drafted is dropped: work runs without one, and home has no entry to prove it by. Weekly and monthly rollups are written only for a period with at least one entry; the ten empty reviews go. The stray day note and template at the calendar root fold into the year or go, and session 4 made your Obsidian daily note the register's diary facet, so the two are one note. *Re-audit* if a facet ever needs a per-day summary that the year map cannot carry.

**`standards/` is four files and a voice library.** `storage-rules.md` (the contract), `user-preferences.md` (the pen folded in, and the durable "how I want things done" — few lines, written by you, loaded every session), `security-and-secret-governance.md` (the standing constraints, today scattered across a brief, a follow-ups file and a CLAUDE.md), `moc-standards.md` (generated), and `voice/` holding the nine on-demand voice rules that sit in `projects/_global/wiki-style/` today. The set is decided here; session 6 capped the always-load tier at 40,000 tokens with the contract's block stripped from the injection. *Why `user-preferences.md` matters beyond tidiness:* 99 of the 101 `preference` notes in the corpus are truncated fragments the miner cut from your sentences. The durable preferences are few, and a file you write beats a corpus mined from transcripts. *Re-audit* if the four files pass a quarter of that ceiling.

**One root map.** `Filing.md` folds into `index.md`; they carry the same write-authority table today. `Ideas.md` becomes a generated list over the idea cards, your preamble kept between markers (session 7). *Why not no root notes, as at work:* the map is for you, and the daemon enforces the table whether or not a note restates it.

**A project's tree converges on work's skeleton.** `charter.md` is the charter (session 4 renamed it from `_index.md`); `decisions/`, `research/` and `drafts/` are the same folders under the same names; `moc-<slug>.md` is generated; `docs/` appears when something mirrors documents. Work's `plans/` is not created at home: a plan belongs to a task and lives in it (session 4, reversing this session's listing). A task lives at `projects/<slug>/tasks/NNN-<verb-slug>/` with `plan`, `progress`, `tracker` and its artifacts flat beside them, and a generated `projects/moc-tasks.md` gives the cross-project view of everything in flight. *Why nest tasks under the project rather than work's flat `agent/desk/tasks/`:* home is a few long projects with many tasks each, and work's flat list has already grown project buckets inside itself within three weeks. `_harness/` dissolves into the skeleton by a mapping table (session 4), paired with the crickets development-lifecycle repoint in three landings. Tasks are never archived: the sequence prefix orders them and the map collapses the done ones (session 5, reversing the archive step this session and session 4 named).

**Naming.** Directories and files are kebab-case with a subtype word where the directory does not carry it: `protocol-` and `recipe-` in `procedural/`; `doc-`, `plan-`, `brief-`, `research-` and `decision-` in a project; task directories verb-first with any external identifier last (`fix-grad-issue-519271308`, not `b519271308-…`). A gate holds it, because work's rule without a gate drifted within weeks into two spellings of one task. Dates appear in a name only where the date is the identity: calendar, scorecards, session traces. An order prefix is not a date: task directories and the files under a project's `decisions/`, `designs/`, `research/` and `drafts/` carry a per-folder three-digit sequence number (session 5).

**Housekeeping the survey found, the agent's to do.** The probe writes at the memory root and the nested directory goes; the vault-root `diagnostics/` was a wrong-root write and goes with its writer repointed; walkers and the empty-directory cleanup treat Drive's `Icon` files as absent; the runner's `health-pass` job, parked at the watchdog's stop rung since 2026-07-25, is revived and the corpus scorecard gets a daily job of its own; the empty reviews stop. Each is recorded in the follow-ups file so a build session can take it.

#### The card (session 1, decided)

What a memory note holds, in the order Obsidian's properties panel shows it: what you read first, what the machinery reads last.

```yaml
---
title: Keep git out of Google Drive's mirrored folders   # the name; the filename is only the address
type: workflow                                            # or kind: for a record; never both
summary: One line — what this is and when it applies.
why: Why this was kept — the reasoning at capture.        # what was happening, and what this decides; only a writer that knows may write it
importance: 7                                             # 1–10; you read it; enrichment proposes, your edit wins
status: active                                            # active | unfiled
lifecycle: active                                         # pinned | active | dormant | archived | superseded
filing_confidence: high                                   # the one confidence tier you read
source: conversation                                      # the transport; source_url / source_id beside it when present
trust: trusted                                            # derived from source, stamped on every note
created: 2026-09-06
updated: 2026-09-06
tags: [git, google-drive]                                 # never an empty list
related: [[drive-upload-staging-churns-transient-files]]  # supersedes / superseded_by live here too

slug: keep-git-out-of-google-drive                        # machine-owned from here down
confidence: 0.91
enriched_by: enrich/1+prompt/a73ff0f4f5dc
enriched_at: 2026-09-06T14:00:00Z
rules_hash: 3c57fd89087c1a25
fingerprint: bd36f748…
importance_proposed: 6                                    # what enrichment proposed; when importance differs, the value is yours
---
```

The body is the memory in prose — your words where they were yours, enrichment's where it wrote them — followed by an optional `## Evidence` block quoting the excerpt it came from. There is no metadata block in the body; the frontmatter already says it.

Field by field, against the corpus of 744 and against the work card (`id · type · title · status · created · updated · importance · tags · related_notes · aliases`):

| field | today | decision |
|---|---|---|
| `title` | on 48% | required on every memory; the miner and `save.py` write one at capture |
| `type` / `kind` | 93% / 7% | unchanged, one or the other, never both |
| `summary` | on 30%, enrichment only | required when the body exceeds a paragraph; written at capture, refined by enrichment |
| `why` | nowhere | new; what was happening when this was kept and what it decides later, one or two sentences. Written at capture by the model in the room or by you, never by enrichment; absent on a candidate (session 2) |
| `importance` | nowhere | new, 1 to 10, the same field you read at work; capture or enrichment proposes it into `importance_proposed`, and a value that differs from the proposal is yours and is never overwritten |
| `status` | 100% | `unfiled` means no judgment has been made, `active` means one has — one meaning for every writer (session 2); `proposed` retires with the opinion lanes |
| `lifecycle` | 97% | unchanged; stamped on the 24 that lack it |
| `filing_confidence` | 14% | the one confidence tier you read, on every note — backfilled `high` where an enrichment scored at or above the contract's floor and `low` below |
| `confidence` | 43% | enrichment's numeric reading, machine block |
| `mining_confidence`, `mining_rationale`, `mining_occurrences` | 15% | retire with the miner's shape (session 2) |
| `source`, `source_url`, `source_id` | 95%, 17%, 2% | unchanged |
| `trust` | 20% | stamped everywhere; it is a table lookup from `source`, so the backfill is deterministic |
| `created`, `captured` | 30%, 26% | one field, `created`; `captured` folds in |
| `updated` | 85% | unchanged |
| `tags` | 84%, 91 empty | an empty list is omitted, not written |
| `slug` | 57% | machine block; always equals the filename |
| `altitude` | 52% | retire; `lifecycle: pinned` is what `canonical` meant |
| `group` | 16% | retire; the path says it |
| `always_load` | 13%, all false | retire; the tier is a directory |
| `evaluator_classification`, `rubric_score` | 15% | move to the watchlist's own record; not a memory field |
| `fingerprint`, `occurrences`, `aliases`, `enriched_by`, `enriched_at`, `rules_hash` | 9% to 43% | machine block |
| `superseded_by`, `supersedes`, `lifecycle_since` | ≤ 7% | unchanged, beside `related` |
| `project`, `task` | nowhere | new (session 4); the project slug and the task's verb-slug the card was captured under, stamped by every writer that knows its binding, taken as optional fields by `memory_capture`; readable fields, placed after `related` |
| `excerpt_edges_unverified`, `review_flags`, `derived_from` | ≤ 6% | machine; `derived_from` gains promote as a writer (session 3); `excerpt_edges_unverified` and `review_flags` are checked for a reader in the card-backfill landing and retired there if none is found |

**Slugs.** The slug is the address and reads as one. `~dup`, `~dup2`, `-1`, `-2` and `workflow-bash-259` are counters standing in for a name; the collision rule produces a meaningful word, and a `~dup` the copies job will collapse is never a file. *Why the card is heavier than work's ten fields:* home carries a lifecycle axis, a transport vocabulary and a trust tier that work does not, and each has a reader in the daemon. Everything a reader does not have is in the machine block or retired. *Re-audit* if a field in the operator-read block goes a month without you or a surface reading it.

**Records** (`kind:` notes — traces, scorecards, maps, reviews) keep the same order for the fields they share and add their own; a record never carries `importance`, `why` or `filing_confidence`.

#### Capture (session 2, decided)

What holds for a memory the moment it lands, and who may write which field. Measured on 2026-09-06 against the live writers: enrichment had run once by hand on 2026-08-27; the daemon's eager trigger was never attached and never fired; the Stop hook's candidates landed `status: active` at low confidence, a state enrichment never reads; and 572 of the 769 notes in the classes were residue from the old miner. The queue the daemon was red about was not a pass falling behind. It was a pass nobody runs.

**The in-session card is the primary capture.** When something durable settles in a session — you say how you want a thing done, a decision is made, a fix is found, or a fact is learned that a later session will need — the agent writes the card immediately through `memory_capture`. It fills the fields you read: `title`, `type`, `summary`, `why`, `importance`, `tags`, and `related`. It lands `status: active`, `filing_confidence: high`, because a writer that could say why already judged it. The cost is a few hundred output tokens per card on the model already holding the context. This replaces a cold model reconstructing the same card from a 200-character excerpt. *Why not a model call in the Stop hook:* it reconstructs too, only sooner, inside a 30-second hook budget against a transcript that can reach a megabyte, and it pays on every session boundary whether or not anything durable happened. It stays named as the fallback. *Re-audit:* a month in which sessions write fewer than one card a day while the traces show decisions being made.

**`why` is the reasoning, and only a writer that knows may write it.** One or two sentences: what was happening when this was kept, and what it decides later. You or the model in the room write it at capture; enrichment never writes, rewrites, or removes it. A card without `why` is a candidate — nothing has judged it worth keeping yet — and it lands `status: unfiled`. The firing miner rule is not a `why`; it goes to the trace. *Why not let enrichment write one:* a reason guessed from the note reads exactly like a real one, and the field is only worth having if it is always yours or the room's.

**`status` means one thing.** `unfiled` means *no judgment has been made*; `active` means *someone or something judged this worth keeping*. The Go writer already meant the first. The Python lane meant the second and wrote `active` at low confidence, which enrichment's gate never reads. Every writer now agrees: a knowing writer's card lands `active`; anything else lands `unfiled`; enrichment's floor moves a note up or leaves it. `filing_confidence` stays the tier you read. No ranker reads it, and no document may imply one does.

**The Stop hook keeps the trace and stops writing fragments.** The session trace is the handoff record, written with no model call. Its `title` is the first prompt; `project` and `surface` are stamped; `touched:` (renamed from `entities:`, which held basenames) lists what was captured and recalled. The body contains `## Asked`, `## Outcome` — the closing recap every session already ends with, which is free and the best summary the session produced — followed by `## Captured`, `## Recalled`, and `## Candidates`. `## Candidates` holds every line the miner once filed as a note, kept as a line with the firing rule, excerpt, and count. The miner files a card only for a HIGH candidate: a durability cue typed by you, under the utterance ceiling, whatever the host says about who sent it. It files that card `unfiled` with `why` empty, so the nightly pass judges it. MEDIUM and LOW never become notes. The four rulings from the write path's labeled sample are built as written. The tally template becomes a gate at the write door, so a stale miner in a worktree cannot refill the purged population. *Why not retire the miner:* the candidates list costs nothing and is the record of what a session said in passing; the next session's model, or dreaming, can promote from it with context a nightly pass over a lone note never has. *Why not keep MEDIUM as unfiled notes:* enrichment scored 85 of the 283 fragments it read at 0.2 or below; a fragment does not become a memory by being rewritten.

**The deep pass is owed once, and the stamp says whether it has happened.** Eligibility moves from status to stamp. A card with no `enriched_at` is owed the deep pass whether `active` or `unfiled`. A card whose body changed since its stamp is owed a light pass. A card with neither is skipped at no cost; the fingerprint key (pass version, rules hash, body) already carries this. What the deep pass does is session 3's. What it may not touch is decided here: `why`, the Evidence block, and an `importance` you set. The machine block records `importance_proposed`; when `importance` differs from it, the value is yours and no pass writes it again.

**The "no" is a demotion.** A note enrichment scores below the floor stays `unfiled` and is listed in needs-review. On a second verdict below the floor — which happens only when the body, the prompt, or the contract changed — the note moves to `lifecycle: dormant`, journaled and in the day's dreaming facet, as the contract already allows for demotion. It leaves the queue, stays on disk, and returns on a genuine recall; from there it runs the ordinary line session 5 set. *Why not archive it at once:* the verdict is about filing, not silence; the silence line archives it in time. *Why not leave it unfiled:* 283 notes unfiled forever is what the daemon was red about.

**Enrichment runs as a nightly batch; the eager trigger retires.** This is the agent's call and reversible. The eager trigger never fired and would spend a model call per capture as it lands. The batch does the same work in one place, under a budget, with a report. Session 3 sets the budget, the order, and the prompt, which drops `altitude` and gains `importance_proposed`. The first real batch runs after that, not before, so the corpus is judged once under the new prompt rather than twice. The daemon's queue-age alert stays red until then, and that is the honest reading.

**The Go capture and the MCP tool.** `memory_capture` gains `summary`, `why`, `importance`, and `related`, all optional, and its description says what `why` is. The surface stays two tools. A capture that names its type and gives a `why` lands `active`; one that does not lands `unfiled`. `instructions` moves into the Go door so the one front door carries the one security rule. The daily write cap stays. `captured` is written as `created`.

**The purge, ruled 2026-09-06 by count.** Six non-overlapping manifests live in the series' working directory under `S2-manifests/`: A tool tallies 292 · B "User stated" fragments 107, including the seven TempleCoordination rules because the 2026-09-05 session's own records carry that ground · C "Fix observed" fragments 21 · D skill-discovery blurbs 116 · E opinion supplements 32 · F `~dup` twins 4. That totals 572 notes, leaving 172: 76 ideas, 47 conventions, 36 references, 5 workflows, 3 fixes, 2 preferences, 3 traces. Each manifest runs only when the day's fresh count matches the ruled count, and the tally gate lands in the same group.

#### Dreaming (session 3, decided)

What runs at night, in what order, what it may spend, and what you read in the morning. Measured on 2026-09-06 against the runner, the binary, the Python cycle and the engine state. No step spent a token, because enrichment is the only code that calls a model and enrichment has no scheduler. Every daily job fires at 13:07, because the runner's `daily` is twenty-four hours since the last run with no hour anywhere. A report-only diagnostic pass resets the binary's clock. The dreaming scorecard named a test fixture as last night's run. The promote job, had it fired, would have filled `crystallized/` with eight targets counted off the traces' recall lists, one of them a test token. The details and the arithmetic are in the series' working file, `S3-dreaming-proposal.md`.

**The night runs in one order, inside a window.** Enrichment first, then the binary, then the Python cycle, then the scorecards and the morning note. Enrichment goes first because everything after it reads what it writes: `type` decides the refile, the body fingerprint decides the copy collapse, `type` and `tags` decide the maps. The runner gains a `window:` field on a manifest — `02:00–06:00` local for the four nightly steps — and a job that falls due outside its window waits for it. The hourly ingest sweep and the shepherds carry no window. *Why not leave the cadence to drift:* a batch registered as `daily` today would spend model calls at one in the afternoon, against the same allowance as your own sessions. *Re-audit* if a night's work ever runs past the window's end; the morning note will say so.

**The budget is a token line, per night, per tier.** One million tokens on the strong tier, or two million on the cheap tier, whichever tier the night runs on, with a call cap of 250 as a runaway guard. Both are printed in the morning note from the batch's own usage, which it now reads from `claude -p --output-format json`; the runner's spend line reads the same field. The first batch under the new prompt is the whole remaining corpus: 171 notes, about half a million tokens. It runs once, by hand, in the window, with `--dump`, and you read twenty before-and-after pairs the next morning before the second night is allowed to run. That is how the corpus is judged once under the new prompt rather than twice. A prompt change re-owes the deep pass to every note, as the pass version already encodes; at the corpus's size that is one supervised batch. *Why a token line and not a call cap:* a call cap bounds the wrong thing — the deep pass is a few thousand tokens a note and the outlier note is thirty — and the allowance you actually pay in is tokens. *Re-audit* when the corpus passes a thousand notes, when a prompt change needs a rollout plan of its own.

**The deep pass runs strong; the light pass is the first cheap-tier audit.** Enrichment consults the tier table instead of naming a model. The deep pass — a note with no stamp — runs on the strong tier because it sets `importance_proposed` and `related`, the two fields you read and the daemon ranks by, and because the tier doctrine routes every unaudited job strong. The light pass — a stamped note whose body changed — may move `summary`, `tags`, `related` and `confidence`, and `title` or `type` only above the floor; it is the job that qualifies a cheap tier, and the first batch is its audit for free: the strong answers exist, and asking the cheap tier the same twenty-five notes is the sample the bar already names. *Why not audit the deep pass for the cheap tier too:* a wrong importance on a permanent card is a wrong rank on every recall until you notice it; the light pass touches nothing you rank by. *Re-audit* if the cheap tier's agreement on the light pass falls under the bar on any later sample.

**The prompt is one string, one hash, two shapes.** The deep pass is handed the note, its five nearest neighbours by the daemon's own search as title and summary, and the contract's prose, which gains a one-paragraph rubric for `importance` you can edit. It returns `title`, `type`, `summary`, `body`, `tags`, derivable `aliases`, `related` chosen from the neighbours offered and never invented, `importance_proposed` on the rubric, and `confidence`. It drops `altitude`. It never returns `why`, and the gate strips one if it comes back; the Evidence block is preserved verbatim; an `importance` that differs from `importance_proposed` is yours and is never sent back. The verdict is session 2's: at or above the floor a note lands `active` at `filing_confidence: high`; below it stays `unfiled` and is listed for you; on a second sub-floor verdict it sinks to `dormant`, journaled and in the morning note. *Why the neighbours in the prompt:* that is the research the card ruling named, it is free, and `related` chosen from a list cannot invent a link. *Re-audit* if `related` on enriched notes is more than a fifth links you would not have made.

**The binary runs at twenty-four hours, activity-gated.** `-every 24h` replaces `168h`; an idle night — no capture, no genuine recall — exits at once, as the gate already does. The maps and the copy collapse then follow the night's judgments instead of lagging a week. Only an applying pass moves the binary's `last done`; a report pass records its own stamp. *Why not weekly, as shipped:* the weekly interval was set when the binary was report-only and the Python cycle still carried the lanes; now the maps are the entry point.

**Crystallized holds syntheses, and a counter never writes one.** A crystallized note is a synthesis written by a model on the strong tier — the tier table already pins `crystallize` — with `consolidated_from` naming every source, and never nightly: it is written by dreaming's weekly crystallize phase, which reads across closed-task outcomes and cards for what has recurred over time (session 4 owns the phase, below), and on request by `/memory crystallize <topic>`, which runs the same phase over what it is pointed at. The `_crystallize-staging/` directory and its fifty-two markers are deleted; nothing was ever composed from one, and the session brief stops counting them. The binary's promote job stops reading `## Recalled`, which is whatever the hook injected, and reads `## Captured` and `## Candidates`, session 2's trace shape; a candidate the miner saw in three distinct sessions is a real recurrence, and what promote writes for it is a *semantic candidate* — `status: unfiled`, `why` empty, `derived_from` naming the traces — for the next batch to judge like any other. *Why not let promote write crystallized entries as built:* recurrence over recall lists is a popularity count of the injector, and today's five traces would have promoted `zorbulax`, `talk`, `progress` and `roadmap`. *Re-audit* when the candidates lists have run a month: if promote has written nothing, the recurrence bar of three is too high for a session-a-day cadence.

**The Python cycle shrinks to what has no Go owner and a reader.** Kept: the lint report; dedup at the shipped 0.92, landing as a *Possible twins* section in the needs-review map with the similarity and both titles, and contradiction triage beside it as a *Shared keys, different bodies* section; facet promotion as a *Proposed facets* section; and the needs-review map itself. The link sweep this session meant to keep had already left the cycle with the vector stack, so there is no sweep to keep. Retired, each with its reason. The lifecycle stage and the calendar rollups: the binary owns both, and two writers on one axis is the shape that produced the superseded-vocabulary plan. Tidying: its shelf lane moves kind-less files after a year untouched, kind-less files are your documents in permanent spaces, and the one move the axis makes — archive, since session 5 — belongs to the binary's lifecycle job. Compression: it walks `supersedes:` chains that no longer exist in that direction and has proposed nothing on any run. The opinion-supplement stage: its lanes retired in session 1. Insight generation and qualification: they write a `kind: insight` candidate into `_dream/insights/` that no surface reads, seven so far, and `_dream/` leaves the vault in landing group 4. The sampled audit: it audits a stage that makes no model call, and it returns with the cheap tier. Entity rollups, stub synthesis and the unfiled drain: the class folded, and the drain is the batch. The confirm-and-revert path for dedup: it took 167 proposals to 11 confirmations in two months. Each retires with a count of what it would have proposed on its last night. *Why not keep the confirm path as the safe option:* a proposal you act on in Obsidian by merging or by writing `superseded_by` is the same act with the revert log replaced by git, which the vault already has.

**Fuzzy pairs are a section, not machinery.** The parking removed in v9.20.0 does not come back: what was removed was a staging directory and a triage engine, and the list it kept is the twins section above. The `fuzzy-merge` model job is not built. *Re-audit* if the section holds more than twenty pairs for a month.

**One morning note.** `agent/diagnostics/morning/YYYY-MM-DD.md` with a `latest` mirror, generated after the last step of the night, and it is the body of the daily email. It replaces the dreaming scorecard and the observability digest email. Four sections, each omitted when empty: *what ran* — the binary's seven rows, enrichment as judged · filed · below the floor · sank · calls · tokens · model, the Python proposals by section, and which steps were skipped and why; *what needs you* — a link to the day's `dreaming` facet in the calendar with the night's counts on one line (session 5), the needs-review sections with counts and the first five items each, and the two forward lists, *sinking within 30 days* and *archiving within 30 days*; *the corpus in one line*, with a link to the day's corpus scorecard, whose parked daily job is revived; and *spend*, last night's tokens and the seven-day total. The observability ladder stops writing a digest that would say "$0 · 0 events", the same rule the calendar's empty reviews got, and its spend line joins the fourth section when it is not zero. The session brief line reads the morning note's first section. The daemon's queue-age alert keeps its three-day threshold; after the first batch it is measuring something real. *Why one note and not the seven surfaces:* on 2026-09-06 the scorecard named a fixture, the digest said nothing, and the brief counted markers nobody would ever compose; a page you read has to be one page.

#### Projects and tasks (session 4, decided)

How a project, a task, a decision and a handoff are represented; what is written when a task changes state; what dreaming adds over time and what it consolidates; how a session opens on a project and knows where it stands. Measured on 2026-09-06 against the live `Projects/` tree. 760 files sit under `_harness/` across twelve projects, 502 of them in archives and 493 with no frontmatter. No tracker file exists anywhere; the plan's Status line does that job, in nine spellings. Zero memory notes carry a `project:` field. Eight handoff files exist, the largest opening with two banners saying it is finished. The orientation read the tails of fourteen progress logs and said nothing about where the project stands. The working file is `vault-perfection/S4-projects-and-tasks-proposal.md` in the agentm project's harness directory; the rulings are its twenty-four questions.

**The words.** A *project* is a directory under `projects/` with a charter and a tracker. A *task* is a directory under `projects/<slug>/tasks/`: one unit of work with a plan, a progress log and a tracker — what the crickets loop calls a plan today. A *step* is one checkbox inside a task's plan — what the loop calls a task today, and the paired release changes "task N" to "step N" in the six commands' prose. The *plan* is the task's intent, written at `/plan` and changed only by re-planning; *progress* is the append-only log, never rewritten; the *tracker* is the living head — objective, state now, next steps, outcome at close — rewritten in place by the session that changes it. A *decision* is a `decisions/` note; a *handoff* is not a file; an *arc* is a set of tasks that closes together. *Why rename the checkbox and not the directory:* "a task's tasks" is the sentence you would otherwise have to explain away, and the directory name is the one you type.

**The project skeleton.** `charter.md`, `tracker.md`, `followups.md` and `roadmap.md` (only where a project has one) at the root, beside the generated `moc-<slug>.md`; then `decisions/`, `designs/`, `research/`, `drafts/` (only where a project drafts), `tasks/`, `completed/` (session 5's name for the finished-work folder, mirroring the others; never tasks) and `desk/`. The charter is yours — what, why, where, what to read first — and the machinery never rewrites it; `_index.md` is renamed to `charter.md` because that is work's name and the two vaults converge. `desk/` holds only what you do not open: the board mirror, `features.json`, `corrections.md`, session markers, and briefs that have no task yet. *Why the tracker and the followups at the root and not in `desk/` as at work:* they are two of the three files you open, and they sit with the third. *Why no `plans/`:* a plan at home always belongs to a task; a directory is discovered, never conjured. *Re-audit* if a project accumulates plan documents that are not tasks — then `plans/` returns for that project.

**`_harness/` dissolves; nothing renames to `desk/`.** Every file moves once, by a table rather than a judgment. A `PLAN-<slug>.md` and its `progress-<slug>.md` become `tasks/<verb-slug>/plan.md` and `progress.md`, with a `tracker.md` written from the Status line and the Goal. A queued plan becomes a task whose tracker says `queued`, and `queued-plans/` goes. An archived plan becomes `tasks/NNN-<slug>/` with a tracker at `done` and `closed:` from its archive date, numbered by its creation order (session 5: tasks are never archived). Briefs, prompts and probes go into their task as `brief.md`, or into `desk/briefs/` when no task exists. `designs/` and the research bundles merge into the project's `designs/` and `research/`. `FOLLOWUPS.md` becomes `followups.md`. `ROADMAP-MASTER.md` becomes `roadmap.md`, with `ROADMAP.md`'s open table folded in by hand while you read the diff. The board and feature ledgers go to `desk/`, the eight handoff files to `completed/`. This series becomes `tasks/NNN-refine-the-vault/`, numbered like the rest (session 5), the first task in the new shape. The repo-local copies (`init.sh`, `verify.sh`, `known-migrations.md`, `.project-mode`) stay where the repo reads them. Task slugs are verb-first, derived from today's plan slugs and listed for your correction before the migration runs; the naming gate holds them after. Nothing is deleted, the structural-move tooling rewrites the links, and `agentmd embed` closes the move.

**The tracker is its own file, and it has one schema.** Frontmatter: `kind: tracker`, `title`, `project`, `task` (absent on the project's own tracker), `status`, `importance` (yours), `opened`, `updated`, `closed`, `issue` when a board row exists, `design` when a living design governs. Body: `## Objective`, one paragraph copied from the plan's Goal at open; `## State`, what is true right now in three to eight lines, rewritten and never appended; `## Next`, the immediate steps in order, the first line being what the next session does; `## Outcome`, written once at close. The plan loses its `**Status:**` line. *Why split it out:* a session opening on a task reads twenty lines instead of five hundred. The board sync, the map, the opening brief and the crystallize phase all read one small file with fields. And the plan stops being rewritten by every session, which is what let the Status line drift into nine spellings on seventy-eight files. *Why not parse the Status line:* a paragraph in a header has no fields, and the temple plan shows it grows into a tracker on its own. *Re-audit* if trackers are found stale as often as the charters are today — then the writer, not the shape, is the defect.

**Five statuses, and what each transition writes.** `queued` (shaped, no session has worked it), `active`, `parked` (stopped on purpose, the reason in State), `done`, `dropped` (withdrawn, the reason in Outcome); today's nine spellings map onto them in the migration. Open, at `/plan`: the directory, the plan, the tracker at `queued` with Objective from the Goal and Next as step one, a progress line. First step, at `/work`: `active`, State and Next rewritten, a progress line per step as today. Park and resume, on your word: the status, the reason, a line. Close, at the last step or at `/release`: `done` and `closed:`; the Outcome, which is the close-out recap the session already writes; the close-out progress line; the board row to Done; and the task's research, briefs and drafts moved to the project's `completed/` by the night. Drop: the same with `dropped`. The task directory itself stays where it is (session 5, reversing this session's move to `archive/tasks/`). The project's tracker is rewritten at every task open and close — its State names the active tasks, its Next the next one — and whenever what the project is for changes. *Why the directory stays:* your ruling in session 5 — a task is short, an archive split across every project is what you did not want, and the sequence prefix plus the map's collapsed done list is the eyeline. *Re-audit* if agentm's forty-seven task directories make the folder unreadable in Obsidian — a `recent` view in the map is the answer, not an archive.

**A decision is a note with a kind; a handoff is a tracker.** A `decisions/` note carries `kind: decision` and one ruling, with its alternative not taken and its re-audit trigger. For agentm and crickets, whose decisions live in living designs' amendment rows, the note points at the row and does not restate it. A handoff is the tracker's State and Next plus the session trace's Outcome. The standalone handoff files go to the project's `completed/` (session 5's name), and no new one is written. *Why:* a handoff file is stale the moment the next session runs and nothing retires it; a tracker is rewritten by construction.

**Every card and trace carries `project:` and `task:`.** The in-session writer, the Stop hook's miner and the trace stamp them from the binding the resolver already knows: the repo's `project.json` names the vault project, and `.harness/active-plan` names the task. `memory_capture` takes both as optional fields. Recall at session start gives a card whose `project:` matches the session's binding a modest boost; the multiplier is the agent's, on the dampen mechanism the ranker already has. The project tracker is itself a note, `kind: tracker`, ranked by `importance` like everything in the permanent space, so "where does agentm stand" finds it on any surface; it reaches the pasted payload through `moc-projects.md`, which the payload names (session 6). *Why not infer the project from tags or path words:* zero notes carry the field and twenty-four carry a project tag; inference has already failed. *Re-audit* when a session works two projects at once — the field holds one value, and the second project goes in `related:`.

**Dreaming enriches a card beyond what the session had.** A session hook lays a card down from what was in front of it: title, summary, `why`, excerpt. The next deep pass makes it much richer from what the session did not have: the neighbours recall finds, the decisions, designs and research that bear on it, the analysis session 3 named. That content is written into the body under its own dated heading (`## Added by dreaming`), below what the session wrote, and never rewrites the captured text or the `why`. The frontmatter moves as session 3 already rules, your `importance` never overwritten. A card with `project:` is enriched with its project's records as neighbours first, which is the difference the field makes. Inside `projects/`, the pass reads everything — tracker, plan, progress, decisions, research — as input, and writes the same additive section and frontmatter into the charter, `decisions/`, `designs/` and `research/` notes. It never writes `tracker.md`, `plan.md` or `progress.md`: those are the session's, and a nightly writer would race the session that owns them. `personal/` keeps session 1's frontmatter-only rule, `personal/ideas/` excepted (session 7). `moc-<slug>.md`, `projects/moc-tasks.md` and `projects/moc-projects.md` are generated nightly by the binary's mocs job, extended from `memory/mocs/` to the projects space; `Projects/index.md` retires into the last. The index keeps a project's `completed/` and ranks it at ×0.30 by its path segment, penalized rather than excluded, and the Go arm reads a bounded head of a progress log as the Python arm already does. *Why additive and never a rewrite:* the captured text is the evidence, and a rewrite that reads well is how residue survived the last purge. *Re-audit* if an added section on a project record is longer than the record — then the pass is writing a synthesis where a link would do.

**Crystallize is a weekly phase of dreaming, and a session never writes one.** A crystallized note is a hard-learned lesson: something that proved itself over time, across more than one task or more than one card, that you would keep after every card it came from has aged out. A session closing a task cannot see that, and a nightly pass over one card cannot either. So the phase runs weekly for now, inside session 3's night window and against its budget line, until its cost is known and a cheaper shape is found. It reads across the Outcomes of closed tasks, the candidate lines the traces and the nightly pass accumulate, and the cards sharing a `project:` or a subject. It looks for recurrence: the same mechanism, failure or method in at least three Outcomes or cards, across at least two sessions, at least seven days apart — your bar. For each lesson it writes one `memory/crystallized/<subject>.md`, a card in session 1's shape with `consolidated_from` naming every source, `project:` where the lesson is one project's, and `why` written by the phase from the recurrence it found, on the strong tier the tier table pins. When an arc closes — the roadmap item moves to Completed, the version line ships, the series ends — the closing session marks it in the project tracker, and the phase's next run writes the arc's synthesis at `memory/crystallized/<project>-<arc>.md` from the arc's Outcomes. `/memory crystallize <topic>` runs the same phase on request. Task close writes only the Outcome, which is the phase's raw material. *Why not the closing session:* it has the task in context but not the time axis; a lesson is what survives the next three tasks. *Why not nightly:* the night is mechanical and cheap by session 3's ruling, and a cross-corpus synthesis is neither. *Re-audit* after three runs: if the phase wrote nothing, the bar is too high for a task-a-week cadence; and when its cost is measured, whether weekly is the cadence to keep.

**A session opens on twenty lines.** The session-start hook prints, for the bound project and task: the project tracker's State and Next, the task tracker's State and Next, the last three progress lines, the count of open followups, and the count of unfiled captures carrying this `project:`. That is under twenty lines, because the surface collapses anything larger unread. The plan is one click away, and the hook says where. `/orient` renders the same brief, then the task table from `moc-<slug>` with in-flight tasks first by importance and the archive collapsed, then decisions and designs by recency; its gloss reads the charter's What line, and it stops reading the progress tails of closed tasks.

**The paired repoint, in three landings.** First, agentm's resolver and writers, backward compatible: `resolve_active_plan` finds a task at `tasks/<slug>/plan.md` and falls back to `PLAN-<slug>.md`, returning the tracker path beside the pair; captures and traces gain the two fields; `memory_capture` takes them; the mocs job learns the projects space; the seven readers of `_harness` accept both layouts; nothing moves. Second, the crickets commands, as the paired release: `/plan` writes the tracker and drops the Status line; `/work` rewrites the tracker at each step and writes the Outcome at close (the directory stays, and the night moves the task's finished records to `completed/`, session 5); `/orient` and the session-start nudge render the brief; `bugfix`, `review` and `release` read the tracker for status; "step" replaces "task" in the prose; the three crickets scripts that compose `_harness` compose the new path; the crickets development-lifecycle design is amended in the same change. Third, the migration, same day, under quiesce: the structural-move tooling applies the table, trackers are written for all seventy-eight units, and the embed backfill runs after. The four open tasks' State and Next are written by hand from their progress tails, and you read them before the move. The resolver's `_harness` fallbacks retire in the following release, once the doctor finds no `_harness/` anywhere. *Why this order:* each landing leaves both plugins and the live vault consistent, and the first two can sit for a day without the third.

**The daily note is the diary facet.** Obsidian's daily-notes format becomes `YYYY/YYYY-MM-DD-diary` under the calendar folder, so the note the daily-note command opens is the register's diary file. The template becomes the diary facet's frontmatter, `## Today`, and a link to the morning note where the retired digest embed sat. The one existing day note moves to `2026/2026-08-10-diary.md`. *Why one note:* two notes for one day, one yours and one the register's, is the split session 1 deferred here, and nothing in the second the first cannot carry. *Re-audit* if you want a day note the register must never touch.

#### Lifecycle per space (session 5, decided)

For every space: what is consolidated, demoted in search, archived, deleted, and untouched by machinery. It sets the thresholds, how you track what moved, and how you undo it. Measured on 2026-09-07 against the live vault and the code. The axis had never moved a note: zero pinned, zero dormant, zero archived, and every journal line was the 09-05 migration. The daemon applied no decay because the switch is absent from live config. The Python arm halved notes every thirty days on a curve the stepped bands were built to replace. Nothing read the contract's five `decay_*` lines. The prompt-submit hook was the only writer to the recall clock, leaving memories claude.ai uses daily over MCP silent to it. Its keys were basenames, and twenty-six named multiple files. Half the corpus carried no date the clock could anchor to. `Personal/Home/Important Docs/` holds recovery codes and certificates, yet it sat in the index and could enter a prompt at reduced rank. The working file is `vault-perfection/S5-lifecycle-per-space-proposal.md`. The rulings come from its §12 across three reads.

**The machinery changes a note's state only where you said it may: the three observational classes, the calendar and the diagnostics.** Everything else is permanent: `projects/`, `personal/`, `standards/`, crystallized lessons, and root notes. They rank and receive enrichment. You supersede or replace them; no pass demotes, archives, or deletes them. Five acts apply automatically where permitted, each journaled and listed the next morning. *Decay* is a rank on the curve without writes; recall undoes it at once. *Demotion* sets `lifecycle: dormant` in place, keeping the note in every search at ×0.30 on top of the curve. *Archive* moves a note to `agent/archive/`, mirroring the memory tree (`agent/archive/memory/semantic/<slug>.md`), stamped `archived`. It leaves everyday search, returning on an explicit archive query or when you move it back. *Deletion* follows archive after an additional wait; the binary writes a manifest first, and the vault's git history preserves the file. *Consolidation* writes a new note from several. This reverses two rulings the arc that built the system held: archive is no longer confirm-gated, and it is no longer never-by-moving. *Why automatic:* your ruling — a stage nobody confirms is a stage that never happens, and the folders it may touch are named. *Why by moving:* Obsidian resolves links by basename, so a moved note is still found by every link that named it, and what the move buys is the eyeline the field never gave you — the class folders hold what is alive, one folder holds what is not. *Re-audit* if a link ever breaks on an archive move; the structural-move tooling rewrites links and would take the job.

**One memory, in sequence.** A card in `semantic/` or `procedural/` that nobody recalls again, by days of silence, every number from the contract plus the last:

| day | what happens | still findable in |
|---|---|---|
| 0 | captured, `active`, full rank | every search, every surface |
| 180 | rank halves on the curve | everywhere, lower |
| 365 | sinks to `dormant`, in place, `lifecycle_since` stamped; ×0.30 on top | everywhere, well down |
| 1,095 | rank an eighth, still ×0.30 | everywhere, near the bottom |
| 1,825 | archived: moved to `agent/archive/memory/<class>/` | an explicit archive query, or Obsidian |
| 2,555 | deleted: manifest under `agent/diagnostics/migrations/purge/<date>/`, journaled | git history, through the deep search |

A genuine recall before archive returns the card to day 0. After archive, serving it on an explicit archive query or moving it back returns it `active` with its clock reset. A session trace follows a shorter line: dormant at 90, archived at 365, and deleted at 1,095 days from creation, clocked from `created`. A trace is the handoff record and the raw material for promote and crystallize, and both have read it inside a quarter. Several categories are exempt from the lifecycle: `lifecycle: pinned` (never decays, never sinks); `type: preference` and `type: convention` (decay, never sink — a rule unread for a year remains the rule, moving only by supersession or your edit); crystallized lessons; and anything outside the three observational classes. *Why exempt the two rule types and not `idea`:* sinking a rule is how the agent forgets how you want things done; an idea nobody has needed in a year is exactly what the curve is for. *Re-audit* when the first dormant transition fires, no earlier than 2027-08, and again when the retrieval gate first shows a decayed note beating an active one on a query you wanted the active one for.

**The mechanics under the table are the agent's calls.** Both arms run one curve from the contract's `decay_*` lines. Go bands stop being constants, and the Python exponential retires. `daemon.decay_enabled` goes on, and the retrieval gate runs before and after the flip. The contract caps demotions at `demotion_cap: 25` per night, draining backlogs across multiple nights. The clock resets on any hit served to any surface: prompt-submit, session-start, `memory_search` over MCP, or `agentmd search`. It never resets on enrichment's neighbour search, the nightly, lint, or the index. The daemon writes the sidecar for its own surfaces. The sidecar keys by path, using fingerprint re-keying on a move. Both sidecars leave `Agent/` for the engine directory. The backfill assigns every note a `created` date: from `captured`, else the vault repository's first commit of the file, else `enriched_at`. Notes the axis cannot see never age. `importance` only ranks; a silent 9 sinks like a 3, and `pinned` is the one word that exempts. Your edit of `lifecycle` in Obsidian counts as a touch. The binary journals it as yours, stamps `lifecycle_since`, and does not re-sink the note that night. A second sub-floor verdict from enrichment sinks a note (session 2, unchanged). The archive move and deletion run inside the binary's lifecycle job, capped like demotion. `purge.py apply` remains the manual lane for manifests you write yourself. The test rule shifts from "nothing under the night deletes a memory" to "every deletion has a manifest and a journal line first". **A deep search reaches the deleted, by request only:** `/memory search --deep <query>` searches the archive, then the purge manifests and the vault repository's git history for what they name, and prints the commit a note lives in, so one `git checkout` restores it; ordinary recall never reaches it. *Why manifests plus git and not a shadow index of the deleted:* the manifest already records every deleted note's path, title and hash, and git already holds the bytes.

**Consolidation leaves the sources where they are, and a lesson outranks what taught it.** Copies collapse nightly to the earliest `created`, the rest marked `superseded`, capped at 25. Twins and contradictions enter a needs-review section for you to merge (session 3). The weekly crystallize phase writes a lesson synthesizing what several cards taught, listing every source in `consolidated_from`. The lesson is permanent learning: it never decays or sinks, and changes only when a later lesson supersedes it or by your edit. Sources must not crowd the lesson out of recall. Once a lesson lands, each source is stamped `consolidated_into: [[lesson]]` and ranks at ×0.30 immediately — matching the demotion `dormant` gives. Sources continue down the ordinary line to archive and deletion, staying in place until then so the lesson's reference links open. Promote writes semantic candidates from traces (session 3). *Why demote rather than archive the sources at once:* a query that names the specific case still finds the card, below the lesson, and the links resolve while the card is where it was. *Why not leave them at full rank:* your ruling — the things that taught a lesson should not affect recall of the lesson.

**Maps and entity views are generated; diagnostics age and are deleted.** `mocs/` pages and future `entities/` living views have no lifecycle. Their generator rebuilds them, removing any page whose members fall below threshold. A hand-written note in `entities/` is a card and ages as `semantic` does. `agent/diagnostics/` has no archive, using retention rules instead: daily digests and morning notes 90 days, three-day digests 180, weekly 365, lint reports 90, scorecards 365, divergence notes 90, monthly digests and `latest_*` mirrors five years, with migration and purge manifests kept. A `retention:` block in the contract holds the numbers. A free `retain` job in the binary applies them, counted in the morning note. Diagnostics rank at ×0.30 as a dampened space in both arms. *Why delete rather than stop indexing:* sixty-five digests in a folder you open for one is the eyeline problem again, and the doctrine that no policy deletes a memory or your document does not cover a digest of Tuesday.

**`projects/` is permanent, and active work is told from finished work by name, by field, and by one folder at each level.** Nothing under `projects/` is demoted or deleted by a pass, and nothing under it runs on the decay curve. A finished project moves to `projects/completed/<slug>/` the night after its tracker reads `done` with its Outcome written. The closing session updates the tracker; the move is bookkeeping. The night rewrites links, journals the move, and lists the project under a collapsed *completed* heading in `moc-projects`. Moving it back and setting the tracker `active` reopens it. The five directories in `Projects/_archive/` land there in group 8. *Tasks have no archive.* A task directory stays at `projects/<slug>/tasks/` for the life of the project. Its tracker's `status` and `closed:` fields indicate whether it is active and when it finished. Its name establishes work order: every task directory carries a per-project sequence number zero-padded to three digits (`tasks/001-refine-the-vault/`, `tasks/002-land-the-purge/`), assigned at `/plan` as one more than the highest in the folder. Obsidian, `ls`, and maps display tasks in creation order. `moc-<slug>` shows in-flight tasks first, collapsing completed tasks under their `closed:` dates. This reverses session 4's ruling that completion moves directories to `archive/tasks/`. *Project documents are ordered the same way:* files under `decisions/`, `designs/`, `research/`, and `drafts/` carry the same per-folder prefix (`decisions/007-tasks-are-directories.md`), assigned by whichever writer creates them and by the night's free `sequence` job for files dropped in without one, links rewritten and journaled. Root files and `desk/` carry no number. The naming gate holds the prefix. This amends session 1's naming rule, which allowed a date in a name only where the date is the identity; an order prefix is not a date. *A project has a `completed/` folder that mirrors it:* `<slug>/completed/` holds `decisions/`, `designs/`, `research/`, `drafts/`, and `desk/` as needed, using original file names. A record moves there when its parent work finishes: research bundles, briefs, or drafts when their `task:` closes; decisions or designs when `superseded_by` is written; the eight handoffs now; and `desk/` briefs when their task exists. The night moves them based on frontmatter and journals the changes. The charter, tracker, `followups`, `roadmap`, launched designs, and standing decisions never move. You move anything by hand in either direction. In both arms, the `completed` segment ranks at ×0.30, matching `archive` and `_archive`. The Python arm's exclusion of `_archive` retires for it. *Why "completed" and not "archive":* the memory archive holds what is retired from use; a project's completed folder holds finished work that is still true — two words for two meanings, one ranker rule for both. *Why a number and not a date for the order:* a date is nine characters and still needs a tie-break for two files from one day; the tracker carries the dates, and the name only has to carry the order. *Why three digits:* two overflow at a hundred, and agentm's research folder holds 233 files. *Rank in the space is importance times activity.* The night computes each project's activity from the past ninety days: sessions bound to it, captures carrying its `project:`, task transitions, and genuine recalls of its records. It writes this into the tracker's machine block as `activity:` with `last_worked:`. Every record under the project inherits that activity multiplier: 1.0 when worked in the last thirty days, 0.7 inside ninety, 0.5 inside a year, and 0.3 beyond, never dropping lower and never changing state. The tracker's `importance` is yours where you set it and computed from activity where omitted; records carry enrichment's `importance_proposed`. `decisions/` keeps the decay exemption both arms already give it, and the rest of the space joins it. A superseded decision moves to `completed/decisions/` and is walled like any superseded note, one click from its successor. Enrichment continues adding sections to project records, including in `completed/`. *Why a project-level signal and not per file:* what you meant by "how much I'm working with a project" is the project, and a per-file clock in a permanent space is decay by another name. *Re-audit* after ninety days of the signal: if a project you consider live ranks below one you consider done, the bands are wrong.

**`personal/` is permanent, yours, and readable.** It has no demotion, policy archiving, deletion, or lifecycle field. You archive by moving notes manually. `Home/_Archive` stays as you keep it, ranking at ×0.30 by its segment. The space ranks at ×0.30 in both arms without a curve, and by `importance` on each file where enrichment proposed one. Background models may read it: the contract's `model_exempt_spaces` loses its `Personal` line. Enrichment reads the space and writes frontmatter only — `summary`, `tags`, `importance_proposed` — never the body, except in `personal/ideas/`, where session 7 lets it add a body section. This reverses session 1's per-area opt-in on your ruling. Temple Coordination records stay under `personal/Church/`, enriched in frontmatter and linked from the project charter. If you want them ranked and enriched as project records, the move to `projects/temple-coordination/` is yours. One folder is walled from recall entirely: the contract adds `recall_exempt_areas:` with `personal/Home/Important Docs` as its first line. It is never indexed, embedded, or served to any surface, remaining on disk and in Obsidian only. *Why a wall beside the dampen and the model gate:* a background pass reading a recipe to tag it is one thing; a foreground recall putting recovery codes into a cloud model's prompt because a query matched them is another, and the second could happen at ×0.30.

**`calendar/` ages on a five-year horizon; `standards/` and the root notes never change state.** A facet file ranks on the curve from its date, with no demotion or archiving. At 1,825 days, a year's facet files are deleted, manifest and journal line first. The year's map and monthly rollups stay as its record. The register gains a `dreaming` facet at `calendar/YYYY/YYYY-MM-DD-dreaming.md`. The night writes this with one line per act: what sank, archived, deleted (with manifest path), consolidated, removed by retention, numbered by `sequence`, or skipped due to hand moves. This records the machinery daily in the register beside your facets. `standards/`, `index.md`, and `Ideas.md` are permanent with no lifecycle. The loader reads `standards/` whole every session, so recall stops serving it, replacing the hook's current deduplication. *Why delete calendar years rather than archive them:* your ruling — one archive folder for the internal space, none elsewhere.

**How you find out what moved, and how you undo it.** The morning note's *what needs you* section links to the day's `dreaming` facet with counts on one line. It keeps two forward-looking lines of its own, omitted when empty: *sinking within 30 days* and *archiving within 30 days*, listing counts and the first five titles. The needs-review map carries both lists in full. A card you open displays its `lifecycle` and `lifecycle_since` properties. To undo an action, move the file back, edit the field, or run a command: an archived note moved from `agent/archive/` back into its class returns `active` with its clock reset and the move journaled as yours; editing `dormant` to `active` does the same; editing `active` to `pinned` exempts it; `/memory revive <slug>` and `/memory pin <slug>` execute these actions from a session; a deleted note is reached through deep search and restored from its commit. Nothing you move or edit is moved or edited back by the night.

**The night follows your hand moves.** You work in the vault during the day, and the night may run concurrently. Whatever you move — a note into `agent/archive/`, a project into `completed/`, or a file between folders in Obsidian or Finder — the night detects and follows. It rewrites links to the old path, re-keys clock and index rows, updates maps, and journals the move as yours. Three mechanisms hold it. A *reconcile* step opens every night before enrichment. It reads the vault repository's rename detection since the last run (the daemon commits the vault, so every hand move is a commit) and the index's fingerprints, pairs each vanished path with the file carrying its body hash, and applies the repairs with the structural-move tooling the migrations use. No job trusts a listing it took earlier. Before each write, the job confirms the file is still at the path with the hash the plan saw. A file that moved in the meantime is skipped and named in the dreaming facet as *moved by hand during the run*, for the reconcile step to repair at the end of the same night. And Obsidian's *automatically update internal links* setting is on in this vault, so a move made inside Obsidian already carries its links; the reconcile step covers moves made outside it and the paths Obsidian does not rewrite. *Why not a lock that keeps you out while the night runs:* the night is four hours in a window you may be awake for, and a vault you cannot touch is not yours. *Re-audit* if the dreaming facet lists a hand move the reconcile step did not repair — that is a missing pairing rule, not a reason for a lock.

**Two loose ends from session 4.** The trust stamp: 620 of 784 cards lack one. Because it is a table lookup from `source`, the group 5 backfill writes it on every note with a `source`. It stamps thirteen memory notes without one as `conversation` where the miner or hook wrote them, and lists any notes it cannot attribute. Records never carry it. The crystallize phase's cost is read from the morning note's *spend* section. It gains per-job lines for enrichment deep, enrichment light, and crystallize, alongside nightly and seven-day totals. The cadence re-audit reads that line after three runs.

#### Surfaces (session 6, decided)

What each surface reads, what it may write, and how the same memory reaches all of them. Measured on 2026-09-07 against the live machine, the live Drive and the code. No client but Claude Code was registered against the daemon, and the Claude desktop app's configuration held no server at all; session 5's line about memories claude.ai uses over MCP was wrong on the transport. claude.ai reaches the vault through the Google Drive connector alone. That connector searches the whole Drive, where two retired vault trees still sat beside the live one, and a full-text search for the commit-trailer rule returned ten files and not the live note. The Gemini rules file on disk was from 2026-07-06 and taught the inbox; the reconciled template from 2026-09-04 already named eight things sessions 1 to 5 change. The daemon's search returned a path and a 24-word snippet with no title, type or summary. The always-load tier was 17 KB a session, a third of it the contract's machine block, and the prompt hook could inject the contract a second time because its dedupe set did not include `standards/`. This session's own first recall returned five session traces of the same pasted brief. The working file is `vault-perfection/S6-surfaces-proposal.md`; the rulings are its fifteen questions, taken one at a time.

**One memory means three reads, in one order, on every surface.** The always-load tier, the project state, and search over the corpus. A surface that can run the daemon gets the daemon's answer; a surface that can only search Drive gets Drive's answer over the same files; a surface that can do neither says so. A surface that can write writes the clock, and a surface that cannot is named as one that cannot. Nothing else is a surface: the morning note and the email are yours to read, and Obsidian is the files.

**The payload is layout-free and pasted once.** The instructions you paste into claude.ai, the Gem and (through the installer) Antigravity stop carrying a folder map. The map is what changes on every landing, and it is why three surfaces have served a July layout since July. The payload names three things that never move: `index.md` at the vault root, which is the map and is current; everything in `standards/`; and `moc-projects.md`, generated nightly with every project's State and Next, with the project's own `tracker.md` and `charter.md` behind it. It carries the reading order, a guide to the card's readable fields (what `unfiled`, `dormant`, `superseded`, `completed/` and `agent/archive/` mean), and the posture. The text is yours, ruled in session 6 and held in the series' working file until landing group 11a writes it into the template; the design's Documentation Plan names both. *Why layout-free:* a surface that looks for a folder that no longer exists finds nothing, says nothing, and answers from its own knowledge, which is the failure the payload was written to prevent. *Why those three names:* Drive search is by title, and each is unique in the vault and stable across every landing group. *Re-audit* if a surface, asked where a capture goes, answers with a path `index.md` does not name.

**`index.md` describes the vault; the twin retires.** The hand-copied `Agent/_meta/how-to-use-agentmemory.md` goes with `_meta/` in group 4, and `index.md` gains a short "how to read this vault" section carrying the same reading order and card guide as the payload without the per-surface part, and naming `standards/storage-rules.md` as the file that decides where a capture goes. An agent that reaches the vault opens the map, and the map explains itself. *Why name the contract rather than copy its routing in:* the payload's reading order is `index.md` and then everything in `standards/`, so a surface following it has both, and the contract is the one gated source of truth for routing — `check-storage-rules` parses it, the daemon reads it at runtime, and the taxonomy growth rule guards it, none of which would cover a second copy in the map. What the map owes is a pointer that still resolves after group 7 folds `Filing.md` into it, since `[[Filing]]` is where the map sends a reader for the address space today. *Why not keep the twin:* nothing regenerated it, and Drive held three copies, two stale. *Re-audit* if a surface given `index.md` and `standards/` cannot say which class a capture lands in.

**One paste, and a re-paste only when the payload's own sections change.** The template lands in the repo first, ahead of every migration, and you paste it the same day. A surface asked about project state before group 8 answers from the charter and says the projects page is not there yet, which the payload tells it to do. After that, a paste is owed only when the posture, the card guide or the surface list changes, and the design owns each of those. The local copies stop being hand-made. The Antigravity rule and the managed section in `~/.gemini/GEMINI.md` are derived from the template by the build under a parity gate; `/memory payload` prints the body and rewrites them; the doctor reports a copy whose hash differs from the template's. *Why not wait for the casing rename and paste once after it:* the layout-free text survives the rename, and a month more of July's map on three surfaces costs more than a paste.

**Four checks prove a paste took**, replacing the four in the follow-ups file. First, on each chat surface in a fresh chat: *what's our commit-message convention?*, answered from the vault with the note's path. Second: *where does agentm stand?*, answered from `moc-projects.md` or the tracker's State; this one can pass only after group 8. Third: *where does a new capture go?*, naming a class and `status: unfiled` and never `_inbox/`, `_always-load/`, `_index.md` or `_harness/`. Fourth, from a session: a Drive title search for `storage-rules.md`, `voice-kernel.md` and `index.md` returns exactly one file each, and the doctor reports the Gemini section equal to the template. You run the first three and the result goes in the series log or the project's followups; the fourth runs from any session.

**The Drive copy is one copy.** You keep the live `Vault` folder in Drive as the read-only copy chat surfaces search, and you remove the two retired trees (`Obsidian/Agent/` and `AgentMemory.pre-rename-backup/`). That is your act on your Drive; the design does not delete your files. The vault's git remote is the NAS and stays off external clouds, so no chat surface reads a git copy.

**The always-load tier is capped at 40,000 tokens, and the contract's block is not injected.** The loader strips the fenced `storage-rules` block the way it strips frontmatter; the daemon reads the block from the file, and no session reads it. A gate in the battery fails when the packaged `standards/` set exceeds 40,000 tokens (your number), the session-start hook prints one visible line when the live tier does, and the loader's own truncation stays as the last resort. The four-file set projected after group 4 is under 4,000 tokens. *Why a ceiling that high:* `user-preferences.md` is the file you write, and a budget that makes you count words there is the wrong pressure; the gate is against creep, not against you. *Re-audit* if the tier passes a quarter of the ceiling.

**`standards/` leaves recall by exclusion.** The contract gains `always_load_areas:` naming `standards/`, and both arms drop hits under it before ranking, the mechanism `recall_exempt_areas:` already uses. The files stay indexed so Drive-side surfaces find them by name; the memory surfaces never serve them, because the loader already read them whole. *Why not dedupe in the hook:* a dedupe has to be repeated in every reader, and one of them forgets.

**The session opens on two blocks, from two hooks.** The always-load tier collapses into a persisted blob and is meant to; the twenty-line brief (session 4) stays visible because it is small. They are not merged: a large hook output is collapsed unread, so the visible line has to come from a hook of its own. The orchestration briefing's inbox and incubator counts retire with the features they counted.

**Prompt-submit serves a trace only on a time question.** A `kind: session-trace` hit is admitted when the prompt carries a temporal bound the hook already extracts, and dropped before the five are chosen otherwise. Traces stay indexed, `memory_search` returns them when asked, and promote reads them. *Why not a score floor:* the fused score is a rank, not a confidence, and a floor silences real hits on a thin corpus. *Why not stop writing traces on an aborted start:* the trace is the handoff record, and the 90-day line retires the useless ones.

**A search hit is the card's readable head.** `memory_search` returns, per hit, `title`, `type` or `kind`, `summary`, `importance`, `status`, `lifecycle`, `project`, then `path`, the score, the dates and the snippet as evidence; `agentmd search` and `/memory search` print the same. It never returns the body, which the surface reads from the file, and never `why`, which is the room's reasoning written for you. The surface stays two tools; `memory_capture` carries session 2's four fields and session 4's two. *Why the head and not the body:* five bodies is the budget the prompt hook already spends; five heads are a screen.

**The daemon writes the clock for its own surfaces; chat surfaces are clock-blind, and the design says so.** Every hit served over MCP or by `agentmd search` resets that note's clock. The recall ledger gains a `surface:` field (`session-start`, `prompt-submit`, `mcp:<client>`, `cli`), written by both arms. A claude.ai read through Drive leaves no mark the axis can see, so a memory you only ever use there ages as if unread. *Why not poll Drive's view times:* the connector's reads do not reliably move them, and a clock with two sources of truth is the basename-key problem again. *Re-audit* the first time a note you used on claude.ai appears in the morning note's *sinking within 30 days* list.

**Claude Desktop becomes a daemon surface.** The daemon is registered in the desktop app as a local server: the same two tools, the same walls, the clock written. It is your account's configuration, so the build documents the entry and the doctor checks for it, and you add it; if the app cannot take the daemon's URL directly, the build ships a stdio bridge, which is the agent's call. Until then Claude Desktop is claude.ai in another window.

**Every chat surface gets one write path, and it is an email door.** A mailbox on a Gmail account made for it receives captures. The hourly ingest sweep gains a reader that polls it and accepts only mail from your own addresses that passes authentication. Each accepted message becomes one card: `type` from a subject tag, `source: email`, `trust: untrusted`, `status: unfiled`, the sender's `why` kept, `project` where named. Everything else in the mailbox is dropped and counted. The sweep's review window and its fixed `instructions` grammar apply unchanged, so the most a spoofed mail can do is sit as an unfiled, untrusted note that never becomes instructions. The nightly pass judges it like any candidate, and the morning note lists what arrived by mail. The Gem's instructions are the payload plus a capture section: it writes the card in the session-1 shape, mails it one card per message, and says what it filed. Its knowledge is the always-load tier and `index.md` as live Drive files. claude.ai does the same through its Gmail connector, so the payload's posture line reads "to keep something durable, write it as a card and mail it to the door" instead of "offer a ready-to-paste note". A mailed idea folds into `Ideas.md` as the sweep already does. The mailbox credential is yours to provide and lives in the engine config beside the SMTP one, never printed. *Why email:* it is the transport the contract already names, the identity seam for mail units already exists in the daemon, and the sweep's staging is built for exactly an untrusted unit. *Why not let a chat surface write the vault:* the read-only posture is the boundary that holds every other ruling. *Re-audit* if a week's mailed captures sit unfiled past the morning note's second listing.

**The deep search is by request.** `/memory search --deep` and `agentmd search -deep` reach the archive, the purge manifests and the vault repository's history (session 5). No hook injects them and no MCP tool exposes them; `memory_search` keeps `include_archived` as its one explicit reach into the archive. A session runs the deep search when you ask for one in words, which is the by-request path session 5 named. *Why:* the deep search reaches what the axis retired on purpose, and a surface that injects it undoes the axis one query at a time.

**The `/memory` skill gains the commands the series names.** `search` (calling the daemon and printing the heads, replacing the stub), `search --deep`, `revive`, `pin`, `crystallize <topic>` and `payload`; `diary` leaves the table and the stale status block is rewritten.

**`Ideas.md` is a list over cards (session 7, decided).** An idea is a card at `personal/ideas/<slug>.md`, flat, in session 1's shape with `type: idea` and an `area:` field taking the areas `personal/` already has (`home`, `church`, `work`, `tech`). The space's rules apply: permanent, never demoted or archived, ranked by importance under the ×0.30 dampen. One rule is different there, on your ruling. Enrichment may write the body of an idea card under its dated `## Added by dreaming` heading, because thinking an idea through is what you want the machinery to do with one. Everywhere else in `personal/` it writes frontmatter only. `Ideas.md` is generated nightly by the binary's mocs job. Your preamble is kept between markers; below it, one line per idea grouped by area carries the card's title and summary and links to the card; dismissed ideas (a `dismissed:` date on the card) sit under a collapsed heading at the end. A mailed idea (session 6) lands as a card there instead of a line in the file. The migration merges two sets into one. The 81 idea cards left in `semantic/` after the purge move to `personal/ideas/`, with `area:` proposed by enrichment for you to correct. Each of the 27 entries in `Ideas.md` becomes a card where none exists, keeping its date as `created` and its cluster as `related:`. The nine incubator graduates keep their research bodies. Measured 2026-09-07: `Ideas.md` was 52 KB with 27 entries and a preamble teaching the retired incubator paths; 91 `type: idea` cards sat in `semantic/`, 34 of them `unfiled` one-line fragments. *Why `personal/` and not a sixth root space:* an idea is yours the way a recipe is, the areas already exist, and a space at full rank beside the classes would reopen session 1's layout and the payload's map. *Why not leave the file hand-kept:* it has been wrong about where ideas go since the incubator retired, and a card can be enriched where a line cannot. *Re-audit* if the generated file passes a screen per area, or if a dismissed idea reappears from a card you meant to drop.

## Alternatives Considered

**Amend Filing v2 instead of a successor.** Rejected. Its body is the record of a build, organized by the six parts that shipped, and the series touches three designs; amending one leaves two stale by construction. It stays inherited and pointed forward.

**Work's flat `agent/desk/tasks/`.** Rejected for home. The flat list is already growing project and activity buckets inside itself at work, and home is few projects with many tasks each. The cross-project view work gets from the flat directory, home gets from a generated map.

**Title Case root spaces.** Rejected. Today's mix is the one state nobody chose; lowercase matches the other vault you read and the kebab rule everything below the root follows.

**A generated `Home.md`.** Rejected in favour of the root map that already exists under its own name.

**Keep the empty `entities/` as a promise.** Rejected; discovered, never conjured.

**Keep the bare-date day index.** Rejected; work runs on facet files and a year map, and home has no entry to justify a per-day file.

**Ten fields, exactly the work card.** Rejected; home's lifecycle, transport and trust each have a reader in the daemon. The fields without a reader are retired instead.

**A model call in the Stop hook as the primary capture.** Rejected for now; it reconstructs from the transcript inside a 30-second hook budget and pays on every session boundary. Named as the fallback with its re-audit trigger.

**Enrichment writes `why`.** Rejected; a guessed reason reads like a real one.

**Retire the miner outright.** Rejected; its candidates cost nothing as lines in the trace and are the record of what a session said in passing.

**Auto-archive a twice-rejected note.** Rejected; `archived` is conspicuous by contract. It sinks to `dormant` instead.

**A call cap as the nightly budget.** Rejected; it bounds the wrong thing. The budget is a token line per tier, with a call cap only as a runaway guard.

**The deep pass on the cheap tier once audited.** Rejected; a wrong importance on a permanent card is a wrong rank on every recall. The light pass is the cheap-tier candidate.

**Promote writes crystallized entries from recall recurrence, as built.** Rejected; it counts the injector, and would have promoted a test token. It reads the trace's captured and candidate lines and writes semantic candidates.

**Rebuild the fuzzy-pair parking.** Rejected; the list is a needs-review section, and the engine's ambiguous band was never measured against a filed corpus.

**Keep the dreaming scorecard and the digest email beside a morning note.** Rejected; a page you read has to be one page.

**Keep the tracker as the plan's Status line.** Rejected; a header paragraph has no fields, and it drifted into nine spellings on seventy-eight files.

**Flatten a closed task into `drafts/`, as work does.** Rejected; blog's `drafts/` holds drafts, and a task's artifacts stay together in its numbered directory under `tasks/` (session 5; session 4 had them under `archive/tasks/`).

**Crystallize at task close, by the closing session.** Rejected after being proposed; the session has the task in context but not the time axis. A lesson is what recurs, and only the weekly phase can see recurrence.

**Enrichment writes frontmatter only in `projects/`.** Rejected; enrichment exists to make a session's card much richer from what the session did not have, and a project record is enriched the same way. `personal/` keeps the frontmatter-only rule.

**Rename `_harness/` to `desk/`.** Rejected; the folder is three of work's folders in one, and it dissolves into them by a table instead.

**Archive by field only, never by moving, as Filing v2 ruled.** Rejected in session 5; links resolve by basename, and one archive folder is the eyeline the field never gave.

**Propose-and-confirm for archive and deletion.** Rejected; a stage nobody confirms never happens. Both run automatically in the folders named, with a manifest and a journal line first, and the vault's git history behind them.

**Leave decay off, as the live install has it.** Rejected; a note that changes nothing for a year and then drops to 0.30 in one step is a cliff, and the curve is what makes the step legible.

**Archive a lesson's sources the moment the lesson lands.** Rejected; the lesson's reference links would break until a reader asked for the archive. They are demoted at once instead and follow the ordinary line.

**A per-file decay in `projects/`.** Rejected; a decision from June is not less true in December. Rank there is importance times a project-level activity signal.

**Per-project `archive/tasks/`, as session 4 ruled.** Rejected in session 5; archives split across every project is what you did not want. Tasks never archive; a sequence prefix orders them and the map collapses the done ones. Finished work moves to the project's `completed/`, and a finished project to `projects/completed/`.

**A date prefix for the order of tasks and project documents.** Rejected; nine characters on every name and still a tie-break for two files from one day. A per-folder three-digit number carries the order; the tracker carries the dates.

**Keep `personal/` model-exempt, per area.** Rejected on your ruling; background models may read it, and enrichment writes frontmatter only. `Important Docs` is walled from recall instead, which is the boundary that matters.

**A lock on the vault while the night runs.** Rejected; the night is four hours you may be awake for. The reconcile step and the per-write re-check follow your hand moves instead.

**Re-paste the September payload as written.** Rejected in session 6; its folder map is already eight items behind this series and would need a third paste after the migrations. The payload is layout-free and points at `index.md`.

**Keep the vault twin of the payload.** Rejected; nothing regenerated it and Drive held three copies. `index.md` describes the vault.

**Dedupe `standards/` in the prompt hook.** Rejected; a dedupe has to be repeated in every reader. The contract's `always_load_areas:` excludes it from recall in both arms.

**A score floor on prompt-submit recall.** Rejected; the fused score is a rank, not a confidence. Traces are served on a time question only.

**Return the body, or `why`, from `memory_search`.** Rejected; five bodies is the budget the hook already spends, and `why` is written for you. A hit is the card's readable head.

**Poll Drive's view times so chat reads reset the clock.** Rejected; the connector's reads do not reliably move them, and a second clock source is the basename-key problem again. Chat surfaces are named clock-blind.

**Chat surfaces write the vault through a Drive or filesystem connector.** Rejected; the read-only posture is the boundary every other ruling stands on. The write path is an email door into the ingest sweep, untrusted and unfiled.

**A Gem that hands you text to paste.** Rejected; that is the Gem you built and stopped using. It mails cards to the door.

**Expose the deep search over MCP or inject it.** Rejected; it reaches what the axis retired on purpose. It runs on your request, from a session or the shell.

**A sixth root space, `ideas/`.** Rejected in session 7; it would rank at full weight beside the classes and reopen session 1's layout and the payload's map. Idea cards live in `personal/ideas/`, where the areas already exist.

**Leave `Ideas.md` hand-kept.** Rejected; the file has taught retired paths since the incubator went, and a line cannot be enriched where a card can. It is generated over the cards, with your preamble kept.

## Dependencies

[Filing v2](agentm-filing-v2.md) for every decision inherited unchanged; [Capture](agentm-capture.md) and [Experience & Dreaming](agentm-experience-and-dreaming.md), which sessions 2 and 3 reconcile; the [storage seam](memory-storage-seam.md) for the root-versus-memory-root distinction the probe and the retrieval gate both got wrong; the live contract in `standards/storage-rules.md`; the crickets development-lifecycle plugin for the `_harness/` repoint and the design template and prose pass; Obsidian's basename link resolution and the Drive sync path, which make a case-only rename a two-step; and the work vault reference in the research bundle, `REFERENCE-work-vault-layout.md`, as the transcription this design compares against.

## Migrations

Landing groups, in the order that keeps the live vault consistent at every step. Session 7 sequenced them on 2026-09-07 into fourteen plans (§ Launch Plans): one per group, with group 8 split at its crickets landing and the night moved up behind the capture writers.

1. **Hygiene.** The probe's root, the vault-root `diagnostics/`, the `Icon`-kept empties, `health-pass`, the empty reviews. No design needed; each is in the follow-ups file.
2. **The purge, and the tally gate.** The six manifests ruled in session 2 (572 notes), run through the migration engine under `--confirm-count`, with the write-door gate that refuses the tally template landing in the same group so nothing refills manifest A. Before any backfill, so nothing dresses residue up again.
3. **The capture writers.** The tool's four fields and `instructions` in the Go door; one meaning for `status` in the Python lane; the trace's shape and the miner's HIGH-only filing; the eager trigger and its never-called attachment removed; enrichment eligible by stamp; the nightly batch registered as a runner job with its budget left to session 3. Writers land before the backfill they define.
4. **The memory root trims.** `_always-load` into `standards/`, watchlists and settings into `projects/agentm/`, engine files out of `agent/`, `_dream/` and `_meta/` out. Path repoints ship with the moves.
5. **The card backfill.** Titles and summaries where derivable, `filing_confidence` from the enrichment score, `trust` from `source`, `created` from `captured`, the retired fields dropped, empty tag lists removed; the 74 notes the Python lane filed `active` at low confidence re-stamped `unfiled`. Dry-run counts first; every backfilled note keeps its `enriched_by` so a model-written title is never mistaken for yours.
6. **Maps and root notes.** `moc-root`, `moc-memory`, the retirement of `Home.md` and the paginated pages, `Filing.md` into `index.md`, `moc-calendar-YYYY`.
7. **The root casing.** Last of the structural moves: quiesce, temp-name two-step per folder, the repoint inventory, link check before and after, `agentmd embed` to close.
8. **Projects and tasks.** Three landings in order: agentm's resolver and writers accepting both layouts, with `project:` and `task:` on captures and traces and the mocs job over the projects space; the crickets commands writing and reading the tracker, the paired release; then the migration under quiesce — the `_harness/` table applied, seventy-eight trackers written, task directories and project documents given their sequence prefixes by creation order, `charter.md` renamed, the handoffs and closed tasks' research into `completed/`, the five retired projects from `Projects/_archive/` into `projects/completed/`, the daily-note wiring, embed backfill — and the `_harness` fallbacks retired one release later. The weekly crystallize phase registers with the night's jobs in group 9, after this group's trackers exist for it to read.
9. **The night.** The runner's `window:` field; the batch job registered with its token line, reading the tier table and printing usage; the two-shape prompt and the `importance` rubric mirrored into the live contract; the binary at `-every 24h` with the report-pass clock fix and promote's new sources; the Python cycle's retirements, each with its last-night count; `_crystallize-staging/` deleted; the morning note and the empty-digest rule; then the first supervised batch, and only after your sample the nightly cadence. After group 3, which lands the writers the batch judges; independent of groups 4 to 8.
10. **The axis per space.** The contract's lines: the `decay_*` bands read by both arms with `daemon.decay_enabled` on behind the retrieval gate, `demotion_cap: 25`, `forget_after_days`, `lifecycle_overrides` for `episodic`, `retention:`, `recall_exempt_areas:` with `Important Docs`, the `dreaming` facet, the `Personal` line dropped from `model_exempt_spaces`; `agent/archive/` created; the archive path class (`archive`, `_archive`, `completed`) in both arms and the Python arm's exclusion retired, its dampen for `personal/` and `diagnostics/` added; the sidecars keyed by path and moved to the engine directory, the daemon writing them for its own surfaces; the lifecycle job's archive move and deletion with the manifest, the `retain`, `sequence` and `reconcile` jobs, the per-write re-check, the activity signal on project trackers; `consolidated_into` from the crystallize phase; `/memory revive`, `/memory pin`, `/memory search --deep`; the two how-tos rewritten. After group 9 (the night) and group 8 (the trackers the activity signal and `completed/` rules read).

11. **Surfaces, in two plans.** *11a, the paste*, independent of every other group and first: the payload template rewritten to session 6's text, the Antigravity rule and the Gemini managed section derived from it under a parity gate, `/memory payload`, the doctor's copy check, the follow-ups' four checks replaced, the retired Python MCP server and its how-to removed; then your paste and checks 1, 3 and 4, and your removal of the two retired Drive trees. *11b, the rest*, after group 5 (the card fields the hit shape returns) and group 8 (the trackers and `moc-projects.md` check 2 reads): `index.md`'s self-description; the loader's block strip and the 40,000-token gate; `always_load_areas:` in the contract and both arms; the trace rule in the prompt hook; the hit shape, the ledger's `surface:` field and the daemon writing the clock for MCP and CLI hits; the mail door (the credential in the engine config, the reader in the hourly sweep, the sender allow-list, the morning note's line, the Gem's instructions and knowledge); the `/memory` subcommands; the Claude Desktop entry documented and checked, added by you.

12. **Ideas.** `personal/ideas/` created; the 81 idea cards moved from `semantic/` with `area:` proposed; the 27 entries of `Ideas.md` written as cards where none exists; the mocs job generating `Ideas.md` between your markers; the embed backfill. After group 5 (the card shape) and group 7 (lowercase `personal/`).

Every group is dry-run first, link-check green before and after, and inherits Filing v2's vault-wins collision doctrine and embed backfill. Rollback for every group but the purge is `git revert` plus the repoints replayed.

## Technical Debt & Risks

- **A case-only rename on a case-insensitive disk under Drive.** Mitigation: the two-step through a temporary name, under quiesce. Re-audit trigger: Drive shows a duplicate folder after the rename.
- **The purge is irreversible.** Mitigation: a manifest per population, an exact count you confirm, the migration engine's `--confirm-count`. Re-audit trigger: any population whose count moves between the manifest and the run.
- **Backfilled titles and summaries are model-written.** A stub enriched into confident prose is how 251 tallies survived the last purge. Mitigation: the purge runs first, and every backfilled field carries `enriched_by`. Re-audit trigger: a backfilled title on a note you would not have kept.
- **Automatic deletion in the memory classes and the calendar.** A policy now deletes what the doctrine said only an operator could. Mitigation: a manifest and a journal line before every deletion, the 730-day wait after archive, the vault's git history behind every file, and the deep search that reaches it. Re-audit trigger: any deleted note you go looking for that the deep search cannot name.
- **A hand move during the night's run.** A job could write to a path you just emptied. Mitigation: the per-write re-check skips a moved file and names it in the dreaming facet; the reconcile step repairs it the same night. Re-audit trigger: a hand move the facet lists as skipped that the next morning has not repaired.
- **The activity bands rank a live project below a done one.** Mitigation: the tracker's `importance` is yours and multiplies the signal. Re-audit trigger: ninety days after the signal ships, if a project you consider live ranks below one you consider done.
- **The switch from no decay to the curve changes every ranking at once.** Mitigation: the retrieval gate before and after the flip, and the curve's first band leaves anything under 180 days untouched. Re-audit trigger: the gate's first regression after the flip.
- **The `_harness/` repoint is a paired release.** Mitigation: three landings — agentm's resolver accepts both layouts first, crickets' commands second, the move third — so both plugins and the vault are consistent at every step. Re-audit trigger: any plan-resolution path that still probes `_harness/` after the fallbacks retire.
- **Seventy-eight trackers written by a migration.** A generated State on a closed task is a summary of a summary. Mitigation: closed trackers carry only Objective, `done` and the archived Outcome; the four open ones are written by hand and read by you before the move. Re-audit trigger: a tracker whose State you would not have written.
- **The weekly crystallize phase writes a wrong lesson into the decay-exempt layer.** Mitigation: the strong tier, `consolidated_from` on every note, the recurrence bar of three across two sessions seven days apart, and the morning note listing every lesson written. Re-audit trigger: any crystallized note you would not keep.
- **Additive enrichment on project records grows them past reading.** Mitigation: the section is dated and headed, and the re-audit fires when an added section outgrows its record. Re-audit trigger: that.
- **The always-load budget grows with the standards set.** Mitigation: a 40,000-token ceiling held by a gate in the battery and a visible line at session start, with the contract's block stripped from the injection (session 6). Re-audit trigger: the tier past a quarter of the ceiling.
- **The email door receives mail from anyone.** Mitigation: the reader accepts only mail from your own addresses that passes authentication, a card lands `trust: untrusted` and `status: unfiled`, the sweep's fixed `instructions` grammar never executes free text, and everything else is dropped and counted. Re-audit trigger: any mailed card that lands `active`, or a dropped-mail count that is not zero on a day you sent nothing.
- **A chat surface's reads never reset the clock.** A memory used only from claude.ai sinks at a year of Claude Code silence. Mitigation: named in the design; sinking is a rank change one recall undoes; Claude Desktop gets the daemon. Re-audit trigger: a note you used on claude.ai in the *sinking within 30 days* list.
- **The payload goes stale again.** Mitigation: it names no folder but `standards/` and three stable files, the local copies derive from the template under a gate, and the doctor reports drift. Re-audit trigger: a surface answering with a path `index.md` does not name.
- **The empty-shell failure can repeat.** Mitigation: `entities/` is not recreated until written, and class populations sit on the daily scorecard. Re-audit trigger: any class at zero thirty days after its writer ships.
- **The in-session card may be sparse.** A capture that depends on the agent noticing a durable moment can miss them. Mitigation: the always-load payload names when to capture, the session brief asks at the end what went uncaptured, and the trace's candidates list keeps what the miner saw. Re-audit trigger: fewer than one card a day over a month while the traces show decisions being made; the Stop-hook model call is the next step.
- **The daemon stays red until session 3's first batch.** Nothing judges the queue between the capture writers landing and the budgeted batch. Mitigation: the alert is reading a true state, and the purge removes 316 of the 356 it counts. Re-audit trigger: the first batch not scheduled within a week of session 3.
- **The first batch rewrites the corpus in one night.** 171 notes judged by one model under one prompt, with `related` and `importance_proposed` on every card. Mitigation: the run is by hand, with `--dump`, and the second night waits on your twenty-pair sample; every write is journaled and the enrichment journal already carries the undo. Re-audit trigger: any pair in the sample where the rewritten body lost a term you would search for.
- **Test fixtures write into the live engine state.** The dreaming scorecard read one as last night's run. Mitigation: the scorecard retires for the morning note, which takes its run id from the runner's marker; the leaking suite is found and isolated. Re-audit trigger: any `/var/folders` path in a vault surface.
- **A stale miner in a worktree.** The hook resolves the miner relative to the session's working directory, so a worktree from an older base runs an older miner. Mitigation: the tally gate at the write door. Re-audit trigger: any note matching a retired template after the gate lands.

## Quality Attributes

### Security

The write-authority table is inherited unchanged: `agent/` written freely, `calendar/` shared, `projects/` under a session grant, `personal/` per task, `standards/` and the root notes on instruction. The one new boundary is enrichment of your own spaces, which writes frontmatter and never the body in `personal/`, except the body of an idea card under `personal/ideas/` (session 7); background models may read that space (session 5), and one folder, `personal/Home/Important Docs`, is walled from every recall surface by `recall_exempt_areas:`. A capture is visible to recall from the moment it lands, at reduced weight while `unfiled`; there is no review window. The guard is that captured content never gains instruction authority, that a fetched or mailed transport carries `trust: untrusted` through every pass, and that recalled memory is data to the surface that reads it. Session 6 adds one door and one client. The email door is the only write path a chat surface has: the reader takes mail only from your own addresses that passes authentication, a mailed card lands `untrusted` and `unfiled`, the sweep's `instructions` grammar stays fixed, and the mailbox credential lives in the engine config beside the SMTP one and is never printed. The daemon's MCP server stays loopback-only with its Host and Origin guards, and registering it in the Claude desktop app adds a client on the same machine, not a network exposure.

### Data Integrity

ID-stability holds through every move: basenames survive the casing rename, the trims and the fold of `Filing.md`, so name-resolved links keep resolving. Backfills are additive and stamped; nothing in your spaces has its body rewritten by a pass; the purge is manifested and count-confirmed.

### Privacy

`personal/` stays exempt from the memory contract. Background models may read it for enrichment, which writes frontmatter only and never the body (session 5, your ruling), with `personal/ideas/` the one exception (session 7). What a foreground recall may put into a prompt is the boundary the contract now draws: `recall_exempt_areas:` names folders no surface is ever served from, and `personal/Home/Important Docs` is its first line. The Temple Coordination records stay under `personal/` and are enriched in frontmatter like the rest of it. What a chat surface can see is the Drive copy of the vault, whole, at your account's permission; the payload's read-only posture is an instruction to the model, not an access control, and the one folder walled from the daemon's recall is still a file in Drive. `standards/` is excluded from recall by `always_load_areas:` because the loader already served it, not for privacy.

### Testability

Each rule lands as a gate: kebab names with a subtype word, the card's field order and required fields, a class directory holding only classes, no empty tag lists, no empty rollups. Every backfill and every move prints its dry-run count before it runs.

## Project management

### Work estimates

| landing group | size |
|---|---|
| hygiene | S |
| the purge and the tally gate | S, ruled |
| the capture writers | M |
| the memory root trims | S |
| the card backfill | M |
| maps and root notes | M |
| the root casing | M |
| projects and tasks | L, paired with crickets |
| the night | M |
| the axis per space | L |
| surfaces, the paste (11a) | S |
| surfaces, the rest (11b) | M, with one credential from you |
| ideas | M |

### Documentation Plan

This design; the crickets development-lifecycle design amended when its paired landing ships; the pointer row and note in `agentm-filing-v2.md`; `Designs.md` and `_Sidebar.md` entries; the vault's `index.md` rewritten when `Filing.md` folds in; `reference/Memory-Daemon.md` for the probe's root; `reference/CI-Gates.md` for the new gates; the how-to for the vault layout once the casing lands; `Capture` reconciled by session 2; `Experience & Dreaming` pointed here by session 3 for the Python cycle's retirements and the crystallization trigger, its body reconciled in the landing group that retires the stages; `reference/Memory-Daemon.md` for the batch's flags and the tier wiring; the runner's how-to for `window:`; `how-to/Tune-The-Archive.md` and `how-to/Archive-A-Finished-Project.md` rewritten to the per-space policy when group 10 lands, and the contract's lifecycle prose mirrored into the live file under that plan's authority (session 5); `templates/agentmemory-context.md` rewritten to session 6's text with the Antigravity rule derived from it, `how-to/Use-AgentMemory-In-Any-Agent.md` and `reference/AgentMemory-Context-Payload.md` rewritten to the layout-free payload, the email door and the four checks, `how-to/Stand-Up-Memory-MCP-Server.md` retired with the Python server, the follow-ups entry on the re-paste closed, and the `/memory` skill's `search` section and status block rewritten (session 6).

### Launch Plans

Approved as final on 2026-09-07 and sequenced the same day into fourteen plans, one per landing group, in the order § Migrations names, with the night moved up to follow the capture writers. That move is the agent's call and reversible: group 9 is independent of groups 4 to 8, and the risk register wants the first batch within a week of session 3. Plan 00, the paste, is the active named plan `PLAN-agentm-vault-00-paste.md` in the project's harness directory; the rest wait in `queued-plans/`, and a coordinator activates each when its dependencies have shipped. Every plan runs in a `/work` session on `opusplan`, deploys the same day it merges, and runs its migration under quiesce after its writers. Nothing goes live all at once.

Your steps, in the order the plans reach them:

| when | your step |
|---|---|
| already done | the Drive cleanup: one copy, check 4's Drive half passing on the gmail account (2026-09-07) |
| 00, the paste lands | paste the template into the claude.ai project and the Gem the same day; run checks 1 and 3 in a fresh chat on each and note the result in the project's followups; check 2 waits for plan 10; the doctor half of check 4 runs from any session |
| 02, the purge | confirm each manifest's fresh count the day it runs, six confirmations, manifest E re-selected first (40 notes in the lanes against its 32 rows) |
| 04, the night | after the first supervised batch, read the twenty before-and-after pairs before the second night is allowed to run; edit the `importance` rubric in the contract if the proposals read wrong |
| 05, the memory-root trims | write `standards/user-preferences.md` in your words; read the drafted `security-and-secret-governance.md` |
| 10, the projects migration | correct the task-slug derivation list; write State and Next for the four open trackers; fold `ROADMAP.md`'s open table into `roadmap.md` while reading the diff; the crickets release that writes trackers ships first |
| 11, the axis | nothing owed; optionally move the Temple Coordination records to `projects/temple-coordination/` |
| 12, surfaces | make the Gmail account for the email door and place its credential in the engine config, never in a session; add the daemon to Claude Desktop per the build's how-to |
| 13, ideas | correct the proposed `area:` on the migrated cards; keep the preamble between the markers |

A re-paste is owed only when the posture, the card guide or the surface list changes, and the design owns each.

## Operations

### Monitoring and Alerting

The daily corpus scorecard gains: residue count (zero after the purge, and a line that stays), class populations, the card-shape gate's count of non-conforming notes, empty directories under `agent/`, and the health-pass cadence itself. The corpus scorecard also lists tasks by status per project and the age of each project tracker's `updated` stamp, and the morning note lists every crystallized note the weekly phase wrote. The morning note carries the night's spend against its token line, per job, the steps skipped and why, a link to the day's `dreaming` facet with the counts of what sank, archived and was deleted, and the two forward lists; a night that ran past its window is a line in it. The dreaming facet names every hand move the run skipped and the reconcile step repaired. The morning note lists what arrived by mail and how many mails were dropped; the session-start hook prints one line when the live always-load tier passes its ceiling; the doctor reports a payload copy whose hash differs from the template and whether the Claude Desktop entry is present; the recall ledger's `surface:` field lets a scorecard count recalls per surface (session 6).

### Logging Plan

Every migration writes its manifest and its dry-run count to `agent/diagnostics/migrations/`; backfills journal what they stamped; the purge writes its manifest before deleting.

### Rollback Strategy

Each landing group reverts with `git revert` in the vault repository plus its repoints replayed; the casing rename reverts by the same two-step in reverse; the purge does not revert and is bounded by its manifest.

## Document History

| Date | Change | Status |
|---|---|---|
| 2026-09-11 | **Amended as plan 04's Python cycle landed (task 5).** Session 3 kept the link sweep in the Python cycle until the first batch showed the deep pass filling `related`. The sweep was already gone: `_stage_link_improvement` left with the vector stack in [PR #435](https://github.com/alexherrero/agentm/pull/435), before this series, so plan 04 had nothing to keep or retire for it and there is nothing to re-audit after the first batch. As built, contradiction triage lands in its own *Shared keys, different bodies* section beside *Possible twins*. The Dreaming body now says both. *Why not rebuild a sweep to honor the ruling:* it ranked candidates out of an index that no longer exists, and the deep pass's `related`, chosen from the neighbours the batch offers, is the linking that remains. *Re-audit trigger:* a month after the first batch, if `related` on enriched cards is sparse enough that a sweep would add links you want. | final |
| 2026-09-07 | **Amended after group 11a's check run, then narrowed by its re-run.** `index.md`'s self-description names `standards/storage-rules.md` as the file that decides where a capture goes; the routing table itself stays in the contract. The first amendment put the routing *into* the map, on evidence from a check run where the Gemini Gem failed check 3 and claude.ai passed it. That evidence did not hold: the Gem's `standards/` had been attached as a folder without the file, and with `storage-rules.md` attached by name the Gem passed check 3 with the routing quoted correctly from the contract. The failing configuration was a surface missing a file it should have had, not a map missing a section — and the payload's reading order gives a surface both. *Why not copy the routing into the map anyway:* it would be a second, ungated copy of the one thing `check-storage-rules`, the daemon's parser and the growth rule all guard in a single place. *Re-audit trigger:* a surface given `index.md` and `standards/` that still cannot name the class a capture lands in. Method note: both outcomes of the re-run were written down before it ran, and both held — check 3 passed once the file was attached, check 1 failed again because its note lives in `agent/memory/semantic/`, which no knowledge set reaches without search. | final |
| 2026-09-07 | Sequenced into 14 plans via /design sequence; first active (PLAN-agentm-vault-00-paste.md), 13 queued in queued-plans/. | final |
| 2026-09-07 | Translated to 14 parts via /design translate: 00-paste, 01-hygiene, 02-purge, 03-capture-writers, 04-the-night, 05-memory-root-trims, 06-card-backfill, 07-maps-and-root-notes, 08-root-casing, 09-projects-writers, 10-projects-migration, 11-axis-per-space, 12-surfaces, 13-ideas. | final |
| 2026-09-07 | Approved as final via /design author review pass. | final |
| 2026-09-07 | Session 7 of the series: the close. The design read end to end; every reversal traced (session 4 over session 1's `plans/` and session 3's crystallize trigger; session 5 over session 4's `archive/tasks/`, session 1's per-area opt-in and naming rule, and Filing v2's confirm gate and never-by-moving; session 6 over session 5's clock ruling and session 2's posture line) and fifteen sentences the reversals had left standing reconciled in place — the handoff files' destination, the close step's move, `_harness/` "becoming `desk/`", `agent/`'s third child, five "is session N's question" pointers, the tidying lane's reason, the machine fields owed a reader check, the Infrastructure table's sessions 4 and 5. `Ideas.md` ruled: idea cards at `personal/ideas/` with `area:`, the one place in `personal/` enrichment may write a body, `Ideas.md` generated over them with your preamble kept; Migrations gain group 12. Your steps written into Launch Plans in plan order. Design plugin note: its sibling lookup fails under the versioned plugin cache, so the gates ran through the crickets `dist/` layout. | final |
| 2026-09-07 | Session 6 of the series: one memory across every surface. The measurement first — no client but Claude Code was registered against the daemon and the Claude desktop app held no server; claude.ai reaches the vault through the Drive connector alone, which searches the whole Drive and found two retired vault trees beside the live one; the Gemini rules file was from July and the September template already named eight things the series changes; the daemon's hit was a path and a snippet; the tier was 17 KB a session with the contract's block a third of it, and the prompt hook could inject the contract twice; the session's own first recall was five traces of the same brief. Fifteen rulings, taken one at a time: one memory as three reads on every surface; the payload layout-free, naming `index.md`, `standards/` and `moc-projects.md`, in your words, pasted once and re-pasted only when its own sections change, the local copies derived under a gate and `/memory payload` printing it; `index.md` self-describing and the vault twin retired; four checks that prove a paste took; one Drive copy, the retired trees removed by you; the always-load tier capped at 40,000 tokens with the contract's block stripped; `standards/` excluded from recall by `always_load_areas:`; traces served on time questions only; a hit as the card's readable head, never the body or `why`; chat surfaces named clock-blind, the daemon writing the clock for its own surfaces with a `surface:` field in the ledger; Claude Desktop registered against the daemon by you; the email door as every chat surface's write path and the Gem re-imagined around it; the deep search by request; the `/memory` subcommands; landing group 11 in two plans. `Ideas.md` as a list over idea cards recorded for a later ruling. | draft |
| 2026-09-07 | Session 5 of the series: consolidation, retirement and forgetting, per space. The measurement first — the axis had never moved a note; the daemon applied no decay because its switch is absent from the live config, the Python arm ran a thirty-day exponential, and the contract's `decay_*` lines were read by nothing; the recall clock had one writer and basename keys that collide; half the corpus had no anchor; `Important Docs` was servable into a prompt. Rulings over three reads: the machinery changes state only in the observational classes, the calendar and the diagnostics; the single-memory sequence (dormant 365 · archived 1,825 · deleted 2,555, traces 90 · 365 · 1,095), archive as a move to `agent/archive/` and deletion both automatic with a manifest first, the deep search by request; the exemptions; one curve from the contract in both arms, cap 25, the clock reset by any surface and keyed by path, every note given a `created`; a lesson's sources demoted at once; diagnostics on a retention table; `projects/` permanent with importance × activity, tasks never archived and ordered by a three-digit prefix, project documents the same, `<slug>/completed/` and `projects/completed/<slug>/`; `personal/` permanent and readable by background models with `Important Docs` walled; calendar on the curve, deleted at five years, the `dreaming` facet as the night's record; the night following hand moves. Reverses Filing v2's never-by-moving for archive, session 4's `archive/tasks/`, session 1's per-area opt-in, and amends its naming rule. Migrations gain group 10. | draft |
| 2026-09-06 | Session 4 of the series: projects and tasks as memory. The measurement first — no tracker file exists and the plan's Status line does the job in nine spellings across seventy-eight files; zero memory notes carry a `project:` field and the trace's field is never filled; eight handoff files, the largest opening with banners saying it is finished; `/orient` reads fourteen progress tails and says nothing about where the project stands; 502 of 760 harness files are archive, all indexed at full rank; a closed task's log is median 2 KB. Twenty-four rulings taken: task and step as the words; the project skeleton with `charter.md` (renamed from `_index.md`), `tracker.md` and `followups.md` at the root, no `plans/`, `desk/` for machine files; `_harness/` dissolving by a mapping table rather than renaming; the tracker as its own file with one schema; five statuses and the transition table; close moving the directory to `archive/tasks/`; `kind: decision`, handoff as tracker; `project:` and `task:` on every card and trace with a recall boost; dreaming's deep pass additive on cards and on project records, never on trackers, plans or progress; crystallize as a weekly dreaming phase reading for recurrence (three across two sessions seven days apart), never a session's act — amending session 3's close-time trigger; the twenty-line opening brief; three paired landings; `roadmap.md`; the daily note as the diary facet. Session 1's `plans/` listing reversed. | draft |
| 2026-09-06 | Session 3 of the series: dreaming, and where the tokens go. The measurement first — no nightly step spends a token because enrichment, the only model-calling code, has no scheduler; every daily job fires at 13:07 because the runner's `daily` has no hour; a report-only pass resets the binary's clock; the dreaming scorecard named a test fixture as last night's run; promote would have crystallized eight recall-list targets including a test token. Seventeen rulings taken: the night's order (enrichment, binary, Python, scorecards) inside a `02:00–06:00` window; the budget as a token line per night per tier (1M strong / 2M cheap) with a 250-call guard, usage printed; the first batch supervised with a twenty-pair sample; the deep pass strong with five recall neighbours as input, the light pass as the first cheap-tier audit; one prompt with two shapes, `altitude` dropped, `importance_proposed` on a contract rubric, `why` never written; the binary at `-every 24h`; crystallized as model syntheses at task close or on request, promote reading Captured and Candidates and writing semantic candidates, `_crystallize-staging/` deleted; eight Python stages and the dedup confirm path retired; fuzzy pairs as a needs-review section; one morning note replacing the dreaming scorecard and the digest email, empty digests no longer written. Migrations gain group 9, the night. | draft |
| 2026-09-06 | Session 2 of the series: initial capture. The measurement first — enrichment had run once (every `enriched_at` dated 2026-08-27), the eager trigger had never been attached, the Stop hook's candidates landed `active` at low confidence where enrichment never looks, and 572 of 769 class notes were miner residue; the red queue was a pass nobody runs, not a pass behind. Seventeen rulings taken: the six purge manifests by count (572 out, 172 remain), the tally gate in the purge's landing group, no enrichment batch until session 3's prompt and budget; the in-session card as the primary capture with the Stop-hook model call named as fallback; `why` as what was happening and what it decides, written only by a knowing writer; one meaning for `status`; the trace as the handoff record (Asked · Outcome · Captured · Recalled · Candidates) and the miner filing HIGH only; the deep pass owed by stamp with `importance_proposed` guarding your edits; a second sub-floor verdict sinking to `dormant`; eager retired for the nightly batch; four optional fields on the tool. Migrations gain the capture-writers group. [Capture](agentm-capture.md) reconciled to the write path in the same change. | draft |
| 2026-09-06 | Created in session 1 of the vault-perfection series, authored from the crickets design template as the successor to Filing v2 (which is pointed here). The layout and the card are decided — twenty-one rulings taken in conversation against the survey of the live vault and the work vault photographed the same evening: lowercase roots, `agent/` trimmed to two children, `memory/` to the classes, tasks nested under their project, `crystallized/` flat, `entities/` folded until written, maps named for what they map with `Home.md` retired, the calendar on a year map, the four-file `standards/`, one root map, kebab naming with a gate; the card's field order with `why` and `importance` added and `altitude`, `group`, `always_load` and the mining fields retired. Inputs for sessions 2 to 6 recorded in their sections: deep-once-then-light enrichment, the `tracker` definition, permanent spaces ranked by importance, personal files enriched in frontmatter only under a per-area opt-in. Prose pass: the cross-model step degraded (`agy` returned an empty result twice), so a Claude-only simplification pass ran against the fact-guard list. | draft |
