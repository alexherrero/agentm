---
title: Autonomy — observability ledger and console
status: launched
kind: design
scope: feature
area: agentm/autonomy
governs: []
parent: agentm-hld.md
seeded: 2026-07-07
approved: 2026-07-07
---

> [!NOTE]
> **LAUNCHED** (2026-07-07). The Autonomy arc's telemetry substrate — a device-local spend ledger, a deterministic console, and a digest ladder into the vault. Cross-cutting, agentm-anchored, points up at the [agentm HLD](agentm-hld.md) rather than one pillar. See the amendment log below for build history.

# Autonomy — observability ledger and console

## Objective

Multi-session autonomy only works if you can see what it cost and what it produced without watching it live. This design gives agentm one telemetry ledger — spend and run events, attributed to plan, task, and model — and a console over it: a local dashboard plus periodic digest notes. The subscription window's own rate limit is the spend backstop; this design gives visibility into subscription use and efficiency across projects.

## Background

This design evolves the budget-governor draft from the Autonomy arc's front-load. The governor paired a spend ledger with enforcement — budget envelopes, spawn denial, an automatic kill path. At review the design kept the ledger and let the host carry the enforcement: on a subscription plan the window rate limit already bounds a runaway, it resets on its own, and the harness's recoverability doctrine files recoverable outcomes under proceed-with-announcement. The arc's standing lock — an orchestrator blind to cost repeats the runaway-token incident at machine speed — is retired by making cost visible: the Mythos fleet failed because no estimate was ever put in front of the operator, and the ledger, the announcements, and the console close exactly that gap.

The evolution also re-formed the ledger's home. The session-cost capture that shipped in crickets' Wave D writes markdown into the Obsidian vault; this design moves telemetry to a device-local event log built for machine records, and the vault keeps the human-facing digest notes. The capability audit's findings carry forward: the substrate decision (resolved to Agent View by `PLAN-autonomy-control-plane`'s opening task — see Risks) and attribution tags are stamped by the harness itself, since the audit confirmed no host carries a plan field for us.

## Overview

Sessions and hooks append small JSON records to a monthly log file on the device. On a schedule, the runner folds those events into a small SQLite rollup with per-plan, per-task, per-model, and per-window tables. Everything downstream reads the rollup: a static HTML console page, a ladder of digest notes into the vault, the fan-out advisory, and — once it ships — dreaming's efficiency trend.

The whole read path is deterministic. The one AI touch is a scheduled analysis pass that writes its findings into the store as another dated artifact; the page renders whatever the store holds, the same way every time.

An unattended run announces its spawns with cost estimates, parks tidily if the window runs out, and ends with a morning report naming what it did, what it spent, and why it ended. The launch-time authorization grade a run states is a control-plane concern (below), recorded here only as one field on its `run-start` event.

## Design

### The event log

One JSON line per event, appended to `~/.agentm/telemetry/events-YYYYMM.jsonl` — outside both the repo and the vault, rotated monthly. An append can never block a session or raise; if a write fails, the session carries on and the line is simply missing. This is the same graceful contract the shipped session-cost capture holds today.

```json
{"ts": "...", "schema_version": 1, "device": "...", "session_id": "...", "parent_id": "...",
 "event": "session-cost | spawn | run-start | run-end | window-park",
 "model": "...", "tokens_by_kind": {"input": 0, "cache_write": 0, "cache_read": 0, "output": 0},
 "cost_usd": 0.0, "tags": {"plan": "...", "task": "...", "arc": "...", "grade": "..."}}
```

Every line carries `device` and `schema_version` from day one — if a second machine ever joins, the aggregator merges logs by device id, and a cheap field now saves a migration later. The harness stamps the plan and task tags at write time from its own active-plan marker and dispatch context. A `run-start` line also records the authorization grade the run was launched under, so the console and the morning report can show what authority it claimed — the grade policy itself lives with the control-plane plan.

### The rollup

A scheduled runner job folds the raw events into a small SQLite file with per-plan, per-task, per-model, and per-window tables, reusing the five-hour bucketing the token-audit analyzer already has. The log is the source of truth and the rollup is derived from it — if the aggregation logic changes, the rollup rebuilds from the events and loses nothing. The health dashboard's history file and the GitHub board mirror stay where they are; the console reads them alongside the rollup, and one read surface is enough.

### The console

A local static HTML page over the rollup, built the way the health dashboard already is. It shows spend by plan, task, and model tier, how full the current window is, and cost per merged PR as a first productivity measure. A shared, published version (a locked-down site, Cloudflare or similar) goes on the roadmap as a backlog note; everything here being static derived data keeps that path simple when its time comes.

### Digests

Each horizon reads the rollup at its own window and lands as a note in the vault's `_briefs/`:

| Cadence | Content |
|---|---|
| Daily | spend and run summary |
| Every 3 days | 3-day rollup |
| Weekly | weekly rollup |
| Monthly | trends across all of the above |

### Delivery — getting it in front of you

A report nobody sees is a report that does not exist. The digests generate on schedule and land in `_briefs/`, a folder at the vault root you have little reason to open — so the whole observability layer stays invisible unless you run `/console` by habit, and it goes dark the moment that habit pauses. **Three channels close that gap, each honoring the device-and-vault-sync privacy rule that permits local and first-party delivery and bans third-party push.**

- **Session-start line — the workhorse.** Every session opens with one line: the latest digest headline, how long ago the last cycle ran, and — the point — a **deadman** that says so when the ladder has gone quiet (*"no digest in 4 days — ladder stalled"*), so a stalled runner surfaces in your next session instead of staying dark for days. It rides `harness-context-session-start` — the same visible hook as the "[agentm] Project state" line you already see — and deliberately **not** the memory-recall hook, whose multi-KB always-load dump the host collapses into a single unread `<persisted-output>` blob (a line there loads into context but is never seen; that mistaken assumption is why the channel appeared broken — see the 2026-07-17 amendment). The needs-your-eye count reads the observability layer's own live signal — runs parked awaiting your resume — until the auto-organization needs-your-eye list ships and widens it. No push infrastructure, and a cycle it has already shown stays quiet.
- **On-device notification — opt-in.** A local OS notification (macOS `osascript` or `UNUserNotification`, a first-party on-device surface) fires once daily with the morning headline and the needs-your-eye count. You opt in per device, and nothing else earns a notification.
- **Daily email — opt-in.** A once-daily email carries the daily digest, for a read away from the machine, and it resolves the email question the digest ladder left open. It rides a first-party mail path you configure — your own SMTP or an on-device mail agent — never a third-party relay. Absent that configuration, the channel skips gracefully and the other two carry delivery.

Together these channels make the delivery-discipline value-gate real: notify-worthy signal appears in one line where you already work, a single daily notification and email carry the rest, and a quiet cycle stays quiet. The FRIDAY arc's "proactive surface" goal lands here. We decline the conversational "save this?" nudge in favor of this delivery, since the auto-triaged inbox, capture-time instructions, and the needs-your-eye list already catch save-worthy moments without interrupting a live session.

### When the window runs out

The window is the backstop, so hitting it should be tidy. When rate-limit errors arrive mid-run, the run parks: state written to disk, progress recorded, and a park note saying where it stopped, when, and the exact command to resume. Morning resume is one paste, by the operator — a run never resumes itself, because a loop that ate one window would simply eat the next one at reset. The morning report names why the run ended — plan finished, gates green, an escalation parked, or the window ran out — with the spend attached.

### The fan-out gate's advisory role

In an interactive session the gate keeps its shipped behavior: it asks the operator before a big dispatch, and they're there to answer. In an unattended run it announces the estimate, logs it as a ledger event, and proceeds. Nothing agentm-side stops, denies, or kills a run over spend; the kill switch stays a lever the operator pulls themselves.

## Alternatives considered

- **The enforcing governor** (the predecessor draft). A subscription has no surprise bill, the window limit already bounds a runaway, and a hard stop keyed to an estimated meter would either halt healthy runs or sit too loose to fire. Retained as the reopening path if metered spend ever arrives — see Risks.
- **Keeping the ledger in the vault.** Telemetry and memory are different things; the vault is curated knowledge on a Google Drive mount with known sync-conflict behavior, and fleet-scale machine records would flood it.
- **One physical store for all observability.** One read surface gives everything a merged store would, without disturbing the shipped health history or board mirror.
- **Auto-resume after window exhaustion.** It would amplify the exact failure the window bounds.
- **A live dashboard service.** Static derived data needs no daemon, and staying static keeps the future shared publish simple.
- **AI in the read path.** The analysis runs on a schedule and writes rows; the rendering stays deterministic.

## Dependencies

- **The shipped session-cost capture and analyzer** (crickets `src/tokens/`) — the writer, reader, five-hour window math, and fan-out gate this design retargets.
- **The runner** — hosts the aggregator and digest jobs as scheduled tasks.
- **The health dashboard** — the pattern (and scorecard machinery) the console page reuses.
- **The harness's active-plan marker** — the attribution source the event writer stamps tags from.
- **The capability audit** (`agent-teams-audit.md`, vault `_harness/designs/post-ag-frontload/`) — the substrate decision it reserved stays with the control-plane plan; its plan-binding finding is answered here by harness-side stamping.

## Migrations

**The session-cost capture moves off the vault.** The Wave D capture writes one markdown entry per model per session into the vault via the memory engine; it retargets to the event log, and the vault write retires. What it touches:

- `session_cost_writer.py` targets the event log; its tests keep the same behavior contract against the new sink.
- `session_cost_reader.py` and the fan-out gate read the rollup.
- The `session-cost` memory kind (agentm's [memory index](agentm-memory-index.md)) goes vestigial.
- Dreaming's efficiency trend is still an unbuilt stub, so pointing it at the rollup is a doc edit.

## Risks & open questions

- **The ledger is an estimate.** The hosts expose no quota API, so accounting works from local transcripts and dispatch announcements, and undercounts mid-session burn. Re-audit trigger: undercount beyond 15% of observable reality moves estimation to transcript-tail sampling.
- **An overnight loop can burn the rest of a window before anyone sees it.** Accepted at review: the loss is bounded, the window resets, and the morning report shows what happened.
- **Enforcement reopens if auto-refilling metered spend ever arrives** — a plan change or an API-keyed fleet. The question reopens from this design's record; until then it stays closed.
- **The substrate choice is resolved: Agent View.** Decided at `PLAN-autonomy-control-plane`'s opening task (2026-07-08), live-verified against Claude Code 2.1.193 (no drift from the capability audit's own baseline). Agent Teams was rejected — one team per session, no nested teams, and no session resumption at all for in-process teammates, all disqualifying for an unattended overnight fleet. Agent View's background sessions (`claude --bg` / `claude agents`) are process-supervised, survive machine sleep and restart, auto-isolate into worktrees, and (as of v2.1.198) auto-commit/push/open a draft PR on completion. See the amendment log for the full record.
- **The Antigravity half of the capability audit rests on third-party coverage.** Re-run it when Google's own docs come up.
- **The shared dashboard needs a publish gate designed at pickup**: aggregates only, personal detail scrubbed, access locked down.
- **Email delivery is decided: a once-daily, opt-in, first-party mail path** (see Design → Delivery), resolving the earlier deferral. The connector is the operator's own SMTP or on-device mail agent, never a third-party push relay; unconfigured, it graceful-skips.
- **The Design section is now as-built, not pending.** The event log, the rollup, the console, and the digest ladder all shipped — `PLAN-observability-ledger` (the aggregator) and `PLAN-observability-console` (the dashboard, digest ladder, park artifact, morning report) both landed, and the control-plane plan wired dispatch + the launch-time grade statement on top. See the amendment log for the shipped detail and the real N1 acceptance run. What's still genuinely open is narrower: the shared/published dashboard (named above), email delivery (named above), and the Antigravity half of the capability audit.

## References

- Code: `session_cost_writer.py` · `session_cost_reader.py` · `analyzer.py` · `fanout_cost_gate.py` (crickets `src/tokens/`) · the health dashboard (`scripts/health/`)
- Designs: [token-audit](https://github.com/alexherrero/crickets/wiki/crickets-token-audit) · [memory index](agentm-memory-index.md) · [model + effort routing](agentm-model-effort-routing.md) · the [HLD](agentm-hld.md) this seeds under
- Predecessor: `BUDGET-GOVERNOR-DESIGN-DRAFT.md` (v1 + v2, both superseded 2026-07-07, vault `_harness/designs/post-ag-frontload/`) · `agent-teams-audit.md` (the capability audit, same directory)
- Built by: `PLAN-observability-ledger` → `PLAN-observability-console` → `PLAN-autonomy-control-plane` (carries the launch-time grade statement + substrate decision) → `PLAN-board-tracking-model` (gated on the control-plane plan's first real team run) — staged in the vault `_harness/queued-plans/`

## Amendment log

**2026-07-17 — The invisible `digest_stale_days` deadman is retired (the follow-up the entry below named).** The entry below closed with *"#320's `digest_stale_days` pending-block entry is now a harmless invisible duplicate of this visible line and can be retired in a follow-up."* This is that follow-up. `orchestration_briefing.py` no longer computes or renders the digest-ladder deadman — the `count_digest_stale_days` / `latest_digest_date` helpers, the `gather_signals` / `_over_threshold` / `build_briefing` wiring, the `digest_ladder_stale_days_threshold` config key, and their tests are gone. `scripts/health/session_brief.py` on the visible `harness-context-session-start` hook is now the single surface for the deadman; the memory pending-briefing keeps its other signals (inbox, watchlist, incubator, idea-ledger, staged-adapt, the nudges), each still tested and unchanged. *Why retire rather than leave it as belt-and-suspenders:* a second deadman on an already-invisible surface adds a vault scan and a config knob every session open for a line no one can read — cost with no signal. *Config safety:* the key came from `DEFAULT_CONFIG`, so the seeded config template drops it automatically, and `_parse_config_md` already ignores unknown keys, so an operator whose config was seeded before this still loads cleanly. *Re-audit trigger:* if `session_brief.py` is ever removed or its hook stops being visible, the deadman has to move back onto a visible surface — do not restore it here (this block is invisible by construction).

**2026-07-17 — The session-start line is now actually visible (the wiring bug the entries below flagged).** The re-audit trigger two entries down said it plainly: *"if the SessionStart line is not actually appearing in sessions, that is a wiring bug to fix before judging the channel."* It wasn't appearing, and this is that fix. The diagnosis: the design's premise — *"the SessionStart hook already assembles a briefing at every session open"* — was half-true. A briefing does assemble (`orchestration_briefing.py`, and #320's `digest_stale_days` deadman rode it), but it is tail-appended to the memory-recall hook's multi-KB always-load dump, which the host collapses into one unread `<persisted-output>` blob. So the line was emitted into context and never *seen* — the operator's "I've never seen it" was exactly right. The fix is a separate visible surface: `scripts/health/session_brief.py` reads the digest ladder's own delivered artifacts (`_briefs/*-digest-*.md` + the park-state files) and emits one line — headline + last-cycle age, or the deadman — from the **already-visible** `harness-context-session-start` hook (the "[agentm] Project state" line the operator confirmed they *do* see). Against the live vault it renders the real stall today: *"no digest in 4 days, ladder stalled (last: 2026-07-13). Digests computed through 2026-07-17 but not delivered — see runner"* — composing this diagnosis with #320's runner fix in one line. *Why not fix `orchestration_briefing.py`'s visibility instead:* a hook's stdout is one stream → one host "success" line, collapsed by size; the recall dump is inherently large, so nothing appended there can be made visible without splitting the hook — a separate small-output hook is the only lever. *Why not a brand-new dedicated hook:* `harness-context` is already registered, already visible, already the agentm session line, and already resolves the source clone the script lives under — a new hook adds settings-merge + global-config surface for no gain. *Scope:* the deadman + headline + last-cycle-age + parked-runs (`needs-your-eye`) half is built and tested; the every-day non-stall notification and the email channel below stay open, and the needs-your-eye count reads parked runs until the auto-organization needs-your-eye list ships. #320's `digest_stale_days` pending-block entry is now a harmless invisible duplicate of this visible line and can be retired in a follow-up. *Re-audit trigger:* if a host ever renders large hook stdout inline (no `<persisted-output>` collapse), the separate-surface rationale weakens and the line could fold back into the memory briefing; re-check the collapse behavior before doing so.

**2026-07-17 — The session-start deadman line shipped ([#319](https://github.com/alexherrero/agentm/issues/319)).** The stalled ladder the entry below flagged had two real causes, not one: `agentm-runner.sh` resolved its job-manifest directory relative to `scripts/` (the `cd` it needs for a sibling import), so every launchd-triggered cycle since the runner was first built discovered zero jobs and exited clean — the digest ladder had never once run from the actual background scheduler, only from manual sessions; and separately, the launchd plist never set `MEMORY_VAULT_PATH`, so a job that did run could silently write into the repo checkout instead of the vault. Both are fixed. This also exposed a marker-honesty gap worth closing on its own: `cycle.py`'s lookback re-anchor wrote the same "status": "done" marker a real completion does, so a job that had never actually run read as healthy — the runner state now carries a separate `last_real_run` field a reader can trust. Of the three delivery channels below, only the **session-start deadman half** of the workhorse line shipped here (`orchestration_briefing.py`'s new `digest_stale_days` signal — "no digest in Nd — stalled", riding the existing pending-briefing mechanism, not a new hook): the every-day headline content (spend, run summary), the on-device notification, and the email channel are still open, exactly as designed below. *Re-audit trigger:* if the digest ladder goes quiet again with these fixes in place, the next suspect is the delivery line itself, not the runner.

**2026-07-17 — Delivery channels added; the FRIDAY proactive-surface goal lands here (F1 design session).** The observability layer generated reports but never delivered them: the digest ladder lands notes in `_briefs/` (a vault-root folder), and the operator confirmed in the F1 session that they had never seen a digest, a review reminder, or the console in normal use — the whole layer stayed invisible outside a `/console` habit, and a real ladder stall (no digest between 2026-07-13 and the session) had gone unnoticed for days (diagnosis chipped separately). Added a **Delivery** subsection with three privacy-honoring channels: a SessionStart brief line with a deadman variant (the workhorse, in-band where the operator already is), an opt-in on-device OS notification (once daily), and an opt-in once-daily first-party email (resolving the long-deferred email question in Risks). This is where the FRIDAY arc's "proactive surface" goal resolves: the daily-brief half is delivery (built here), the delivery-discipline value-gate is these channels plus quiet-stays-quiet, and the conversational "save this?" nudge is declined — the auto-triaged inbox, capture-time instructions, and the auto-organization design's needs-your-eye list already catch save-worthy moments without interrupting a live session. Also corrected the Digests section's stale `_inbox/` to the as-built `_briefs/` (the F2 fix during the V8 proving phase). *Re-audit trigger:* if a channel proves noisy in real use, tighten its threshold; if the SessionStart line is not actually appearing in sessions, that is a wiring bug to fix before judging the channel (the operator reported never seeing the existing briefing line — a build-side check, not a design gap).

**2026-07-15 — Unattended merge-on-green needs a clear global permission path; provisioning is dev-setup's, detection is doctor's.** The N1 retest (vault `_harness/designs/v8-proving/PROVING-LEDGER.md` item 19) drove a real unattended dispatch all the way to `gh pr merge`, then stalled: the operator's *global* `~/.claude/settings.json` carried `Bash(gh pr merge:*)` in its `ask` list, and Claude Code resolves permissions `deny > ask > allow` — an `ask` entry beats any `allow` at any scope, so no project- or job-scoped allow could clear it (which is why the project-scoped override, agentm `052de06`/`a279734`, was reverted). The working fix is a *move* — remove the rule from the global `ask` list, add it to `allow`. Decision on where that provisioning lives: **dev-setup owns it** — its `capture.sh` already declares dev-setup the sole owner of the `permissions` block in `~/.claude/settings.json` (agentm owns only `hooks` there), so a new idempotent `ensure_gh_pr_merge_allowed` step (mirroring its existing `ensure_claude_co_authored_by_disabled`) is the natural home. **agentm's install.sh stays silent** on the global permission surface — a project-level install silently deleting a user's global security-gate entry is past the trust line, breaks that ownership seam, and over-provisions for the ~all agentm users who never run unattended merge. **agentm's doctor detects and points**, never writes: `machinery_doctor.py`'s `check_unattended_merge_gate` WARNs (with a copy-pasteable remedy — the opt-in `scripts/enable-unattended-merge.sh`) when `n1-overnight` is registered but the global gate would block, matching doctor's "reports gaps, never mutates state" contract. *Why not agentm owns it:* it can't be done correctly without editing/removing a guardrail from a stranger's global config during a project install. *Why not a runner-injected scoped grant:* wrong locus (the operator's own ruling put the risk on "running the fan-out/merge flow at all," not per-repo) and mechanically a scoped `allow` can't override a global `ask` anyway. *Re-audit trigger:* if Claude Code's permission precedence ever changes so a more-specific scoped `allow` can override a broader global `ask`, the scoped-grant option reopens — re-verify the `deny > ask > allow` assumption before relying on it. Full reasoning: vault `v8-proving/unattended-merge-gate-verdict.md`.

**2026-07-08 — Board tracking model decided: substrate-native shape, no by-agent axis (`PLAN-board-tracking-model` task 1).** With the control-plane's real N1 run as evidence, the operator and this session decided the tracking-model question the 2026-06-19 A1 verdict and the 2026-07-03 field-taxonomy rethink (ROADMAP-MASTER § ⑫) both left open: there is no agent-identity concept anywhere in the dispatch substrate to track by — Agent View names a session `<plan>-<task>` (`n1-demo-1`, `n1-demo-2`), never an agent or persona identity. What every dispatch *does* carry is a `tier` (`classify_work_type()`'s T0-Mechanical…T4-Deep), already real and populated, that nothing on the board tracks today. Decision: **no net-new by-agent tracking** (there is nothing for it to track) — repurpose the board's `Track` single-select field for **dispatch tier** instead of version (per ⑫'s own finding, versions already live in the issue hierarchy, a job `Track` was redundantly also doing). The field-taxonomy mechanics (option-set migration, `board-items.json` + renderer reconciliation) land in `crickets-github-projects.md`, its designated home per ⑫, in task 2. *Re-audit trigger:* if a real agent/persona-identity concept is ever added to the dispatch substrate, this decision is the one to revisit — it was made against "no such concept exists," not "such a concept exists but isn't worth tracking."

**2026-07-08 — Acceptance demo closed: the N1 rerun ran for real (`PLAN-autonomy-control-plane` task 6).** `PLAN-autonomy-control-plane` merged (#251) with task 6 escalated rather than simulated — a sandboxed build session's real dispatch test found `claude --bg` sessions couldn't authenticate there, and the plan's own constraints forbade faking the demo. The operator ran it for real afterward, from their own terminal, against a clean `origin/main` worktree: `n1_run.run_n1_sequence()` dispatched two real Agent View background sessions tagged `plan: n1-demo`, declared the launch grade (`G-ship`) on a real `run-start` event, and both sessions completed. The harvest step surfaced the actual root cause behind the earlier sandbox finding — not a sandbox artifact at all, but the base `claude` CLI never having a persistent login (`claude auth status` read `loggedIn: false`); the operator's regular usage runs through a session-managing host that injects auth per session, which a `--bg`-spawned process doesn't inherit. `claude auth login` fixed it for the base CLI same as it would for any machine in this state. Once fixed: the aggregator built a real rollup, the console rendered it, and the morning report read *"Ended because the plan finished (plan-finished). Spend: \$0.4607"* — matched exactly by an independent grep across the raw event log (`$0.1672 + $0.2935 = $0.4607`, plus four `$0.00` `<synthetic>`-model rows from the pre-fix failed attempts, priced at zero by the pricing table's own graceful-unknown-model fallback rather than crashing). Morning report, console rollup, and raw ledger all agree — the plan's own acceptance criterion, met. `PLAN-board-tracking-model`'s gate ("the control-plane plan's first real team run") is now satisfied. *Re-audit trigger:* none outstanding for this plan; the auth-setup step (`claude auth login`) is worth naming in onboarding docs for any other machine that dispatches through Agent View for the first time.

**2026-07-08 — R1 resolved: Agent View (`PLAN-autonomy-control-plane` task 1).** The capability audit (`agent-teams-audit.md`, vault `_harness/designs/post-ag-frontload/`) reserved the fleet-substrate decision rather than resolving it. This plan's opening task made it explicit: re-verified live against the installed Claude Code (2.1.193, unchanged from the audit's own baseline — no version drift to re-check for) that `claude --bg` / `claude agents` (Agent View's background-session primitive) is real, current, and behaves as the audit described (process-supervised; `claude agents --json` confirms the live session schema is `{pid, cwd, kind, startedAt, sessionId, name}` — no plan-ID field, confirming the audit's R2 finding independently). Agent Teams was rejected on the audit's own findings: one team per session, no nested teams, and — the disqualifying one — no session resumption at all for in-process teammates, which a fleet meant to survive an unattended overnight stretch cannot tolerate. Antigravity's equivalent stays out of scope for this decision; the control plane is Claude-Code-first, matching the "Claude-first scheduling" precedent already set elsewhere in this harness (crickets' wiki-watch) — Antigravity support is a forward reference, not a rejected option. *Re-audit trigger:* if Antigravity's own docs become reachable (the audit's own named gap) and the arc later needs cross-host dispatch, re-open this decision for that host specifically; it does not reopen the Claude Code half.

**2026-07-07** — Seeded at AA2 as the evolution of the budget-governor draft, lifted from the approved vault draft (`OBSERVABILITY-DESIGN-DRAFT.md`, `status: final`, `approved: 2026-07-07`) after two operator review passes: the first dropped enforcement (subscription billing has no surprise bill; the window limit is the host-run backstop), the second homed the authorization-ladder remnant — the launch-time grade statement — to `PLAN-autonomy-control-plane`, leaving this design only the grade recorded on `run-start` events.
